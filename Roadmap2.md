# Roadmap 2: the programme after 6.3.2

**Written 14 September 2026.** [Roadmap.md](Roadmap.md) is the record: 864 rows done, a handful open, three dozen deferred with their reasons. It has grown into the history of the project, and it is kept because every row in it is a claim that was checked. This file is the plan: what still stands between this server and the best mail server there is, held against what the people who run Gmail, Outlook.com, Exchange, Dovecot and Stalwart offer, and worked through in the order below. A row moves out of here into Roadmap.md when it is done, with the measure that says so.

The legend is Roadmap.md's: ✅ done and verified, 🔄 underway, ⬜ not started, ⏸️ deferred with the reason written down.

## 1. Landing what was built on 13 and 14 September

| | Item | Where it stands |
|---|---|---|
| ✅ | **Linux suite waves A to F** (PR #251) | Merged 14 September, 02:46. Windows 2,233 / 2,225 passed / 0 failed / 8 skipped; Linux 1,457 / 1,185 passed / 0 failed / 272 skipped. Schema 6039: a domain's relay password could not be saved on any Windows installation, found by the first honest gate of the branch. |
| ✅ | **One header on every source file** (#256) | On master 14 September, 05:02, by the direct push. |
| ✅ | **The tidy of the two Copilot drafts, and the two defects its own gates found** (#255) | On master 14 September, 05:02. Gated green at the third attempt. The first gate died in `generate-com-wrapper.ps1` under Windows PowerShell (MIDL's stderr as a terminating error); the second on a recordset walk that a COM error could end with a minidump - both fixed on the branch. |
| ✅ | **The live update from an open Control Panel** (#258) | On master 14 September with batch 2 (#259): its own gate was green at f124e5e09, and the batch's at a9261d7eb (2,267 tests, 2,259 passed, 0 failed, 8 skipped). It carries one more fix, from batch 1's Linux run: a message expunged between the indexer listing it and its terms being saved is no longer reported as an error (PostgreSQL's foreign key refuses the save; SQL Server Compact accepted it). The first real update, 6.3.1 to 6.3.2, failed twice with Inno's exit code 5 because the Control Panel that started it kept its own files open. The helper now ends what runs from under the installation, the Control Panel closes itself, and a failed run quotes the installer's log. |
| ✅ | **Linux suite waves G and H** (#254), **CardDAV** (#253), **the webmail rebuilt** (#257), **four more webmail features and twelve search operators** (#259) | On master 14 September as one batch, pushed whole after the full Windows suite on a9261d7eb: 2,267 tests, 2,259 passed, 0 failed, 8 skipped. The batch's first gate failed two CardDAV tests - a comma-bearing TYPE list quoted on the way out, so a card the webmail had edited came back with `TYPE="INTERNET,PREF"`, and a 4.0 card a test still expected served back as 3.0 - fixed in the batch's last commit, the collection now advertising both versions. |
| ✅ | **The Deck census, the REST write routes it found missing, and the Deck views** (#260) | On master 14 September with the fourth batch, whose gate covered it: its own full Windows suite on 1e549fe9f (2,279 tests, 2,268 passed, 3 failed, 8 skipped) failed three tests the next commits put right - two in the account update's new fields (two tests wanting two sentences for a bad expiry date, and an expiry of today expected to leave the message on, where the read has always switched it off), one a test's timing - and its hosted Linux run (1,493 tests, 1,246 passed, 2 failed, 245 skipped) two more - **on PostgreSQL every value with a backslash was stored with it doubled** (the escaper doubled it unconditionally, right only while `standard_conforming_strings` is off, and it has been on by default since 9.1; the connection now reads the server's answer and doubles only where the server escapes), and the pipelined 222 KB chunk test's timing. Its CI had found two Linux-only compile errors on the way (an include spelled `PersistentDNSBlackList.h` for a file named `PersistentDNSBlacklist.h`; the Linux suite's shims lacking the anti-virus group and nine domain properties), and it carries two fixes besides: the container image job of a tag run, which asked docker for `Progressiverobot/hmailserver` and was refused the capital letter at 6.3.2; and **issue #261** - an outbound BDAT chunk larger than one send buffer stalled after the first buffer, because the second was queued behind the reply's read in a queue that starts only its head, so every message over 60,000 bytes to a CHUNKING remote had timed out since outbound BDAT arrived (the queue runs once more after a read starts; two 222 KB tests). |
| ✅ | **The webmail's next six features, the Deck to parity, and the Linux suite's last route families** (#262) | On master 14 September as the fourth batch - three branches built in parallel by three agents off the third batch's tip and chained, the last rebase's conflicts all of the both-added kind - after the full Windows suite on its tip: the numbers are on the wiki's Changes page, section 4d, written once the batch had landed; the hosted Linux run on the same tree: the numbers are on the wiki's Changes page, section 4d, written once the batch had landed. |
| ✅ | **6.3.3** | Published 15 September 2026 as the latest release, build 42, schema 6040. Cut from master plus what the adversarial review of the day confirmed (the REST ini routes answering to the administrator password only, a domain-scoped key refused a domain's limits, the CardDAV entity scan and multiget bounded, MySQL's card column widened) and the fix for issue #263, which had stopped every upgrade from a schema older than 6038 in 6.3.2. The assertion build's suite first: 2,302 tests, 2,294 passed, 0 failed, 8 skipped; then the stamped shipping binary's: 2,302 tests, 2,294 passed, 0 failed, 8 skipped; three clean builds of the tree produced the same `hMailServer.exe`. Installer `hMailServer-6.3.3-x64.exe` SHA-256 `45A9D7F1A78BB31031DDE27849FA21828A60AFF178F32CBC2B0D9CD93F6B33F2` as built, Authenticode-signed on publication; the six Linux packages, two SBOMs and the cosign bundles attached by the release workflows, the smoke test green before it was published. The notes say that an installation on 6.3.1 or 6.3.2 must be updated by hand once, since the helper that runs a live update is the one already installed; the wiki's changes page carries the release as section 4d and the schema steps 6039 and 6040. Issues #261 and #263 closed. |

## 2. The Linux control panel: the browser Control Deck to parity

The desktop Control Panel is WPF and runs on Windows only; that will not change, and it should not - it is the best-in-class Windows administration program this server has. On Linux the administration surface is the browser Control Deck served at `/WebAdmin`, the REST API (81 routes, 40 of them administrative), and `hmailserver --set-admin-password`, `--create-database`, `--upgrade-database`. The Deck today has ten views - dashboard, domains, delivery queue, logs, rules, routes, certificates, ports, TLSA, settings - which is far more than Roadmap.md's row on it still says, and far less than the desktop program. Parity is the item.

| | Item | Detail |
|---|---|---|
| ✅ | **A census: the desktop Control Panel's every page and field against the REST API and the Deck** | **Landed 14 September (#260).** `build/check-deck-parity.py`, run in CI, and `hmailserver/docs/DeckParity.md`: 335 properties the desktop writes; 306 writable over REST, 248 reachable from a Deck view, 29 missing over REST and 58 over REST that no Deck view reaches - from 240, 153 and 95 at the start of the day. The row as written: A script under `build/`, run in CI like the settings-index check, that lists each COM property and Control Panel control and whether a REST route writes it and a Deck view shows it. The number it prints is this section's measure; it reaches zero when the section is done. |
| ✅ | **The REST write routes the census finds missing** | **Landed 14 September, in two batches (#260, #262):** the anti-virus settings group, sixteen more account fields, nine more domain fields, the distribution list's settings and a `PUT` for it, DNS blacklists, SURBL servers, white-list addresses, blocked senders, incoming relays, blocked attachments and the greylisting white list as resources, the cache group's eight ceilings and lives, an IP range's expiry, `abort_spam_flagged` on a rule's forward and reply, `user_interface_language`, application passwords and folder permissions administered under the account's address; 95 missing became 2 - groups and their members - and on 15 September the groups resource landed with the batch after 6.3.3, so the census now reads **330 of 330**: every property the desktop program writes is writable over REST, and what is left is the Deck's own reach (322 of 330, the eight being folder permissions, an application password's name and active flag, and a group's name and membership) - after the census was corrected for what it had misread (a diagnostic's inputs, a parent's id, a name that is a key). The row as written: Each validated by the code that owns the value, as the settings, rules, routes, aliases, accounts, certificates and listeners already are. Likely: distribution-list members and moderation, per-account rules and vacation, DKIM keys per domain, IP ranges' every field, backup and restore, archive holds, quarantine release, API-key scopes, the anti-spam and anti-virus settings in full. |
| 🔄 | **The Deck rebuilt as one page the way the webmail was** | **The reach half is done, 14 September (#260, #262):** a harness of its own (`build/check-deck-script.py` and `build/deck-script-test.js`, in CI), full domain editing, an IP-ranges view with expiry, fetch-account and backup views, the account editor in full (28 keys, grouped as the desktop groups them), distribution lists and aliases under their domain, the seven small collections from one description, server messages, the scripting, cache and indexing groups with their verbs, and the rule editor’s abort flag. **The shape half is done, 15 September.** The look is the webmail’s rather than a second design language: one `:root` block carries the palette `Portal.html` declares, value for value, with `[data-theme="dark"]` beside it carrying its dark theme, and the spacing scale, the three radii and the type ramp are the desktop console’s from `ControlPanelDesign.md`; every colour the page paints with comes from one of those two blocks, and the harness fails on a literal anywhere else. The theme is light, dark, or the system’s - `prefers-color-scheme` asked through `matchMedia` and followed as the reader changes it, a choice made overruling it until it is given back, and the choice kept in `localStorage`. The frame is the console’s, in the Deck’s own markup: a command bar (the brand, the view, the search, the link as a status pill with its colour, its mark and its word, the theme, the shortcut list, signing out), a navigation column that becomes a rail of glyphs under 900 pixels with each view’s name as its tool tip, and a content area of cards, grids, tables, form rows, empty states and notices - the notice being the console’s `InlineNotice`, its level, mark and severity word now carrying the ports view’s restart sentence and the groups view’s delete sentence, both in the server’s own words. It is usable with no pointer at all: a skip link past the navigation, `/` to the search box (which hides the rows of a table and the rows of a form that do not carry what is typed, and reads the settings and messages filters alongside itself rather than fighting them), `?` for the list of the shortcuts themselves, Escape to close that or leave the search, the arrows and Enter over a table’s rows, the arrows between the views while the keyboard is in the navigation column, and a visible ring on everything that takes focus. And a groups view, which is the last thing the census counted as writable over REST and reachable from no view here: the groups listed, created, renamed and deleted, and the accounts in one added by address and removed by the account’s id, against `/api/v1/groups` and `/api/v1/groups/{id}/members` - so the census reads **324 of 330**. Thirty-six new texts, translated into all twenty catalogues in the same commit (536 keys), and the harness is at **390 checks**, from 336. **What is left:** the six properties no view reaches - a folder permission’s four fields, and an application password’s name and active flag - and the views this row names that the page still has none of: the diagnostics probes, quarantine, metrics history, the update page, archive and legal holds, and external setup. The row as written: The same design tokens, dark theme and keyboard as the webmail; a harness that runs its script like `build/portal-script-test.js`; served from the binary so the file is the file checked. Views for everything the desktop program has: accounts in full (forwarding, vacation, signature, rules, quota, two-factor and application passwords, directory link), aliases and lists, per-domain DKIM/DMARC/MTA-STS/TLS-RPT, IP ranges, auto-ban and lockout, spam and virus filtering, backup and restore, archive and legal holds, quarantine, API keys, metrics history, the update page, diagnostics (MX, SRV, DANE, DNSSEC, TLSA probes), live logs, external setup. |
| ✅ | **The Deck in the twenty languages** | **Done 15 September 2026.** Twenty, not seventeen: the webmail speaks twenty and the Deck now speaks the same twenty, from catalogues of its own - 501 texts in `hmailserver/installation/WebAdmin/languages/<code>.json`, in the form the webmail's catalogues keep, written against the register and the words the webmail chose (of the 28 texts the two pages share, between 21 and 27 read identically per language, and those that differ differ by sense). One mechanism for every text a reader sees: the sentences the script builds go through `t()` and `tf()`, a table's hints, titles and captions are marked with `K()` and looked up where they are drawn, and the static markup is walked once a catalogue is known, each node keeping its English so the walk can be undone - so English needs no catalogue and no request. The language is the browser's, or the one chosen in the switch the header and the sign-in card carry and this browser remembers, with English as the fallback for anything a catalogue lacks. They are files beside the page because the page is a file beside the binary: the Windows installer, CMake, the AppImage and `post-build.bat` all ship the directory, `build/build.ps1` puts it beside a locally built server, and the server serves one at `GET /deck-lang/<code>.json`, unauthenticated as the page is, reading the bytes from disk through the helper the page already used - a round trip through the ANSI code page would have served question marks. `build/check-deck-languages.py` holds every catalogue to the page as `check-portal-languages.py` holds the webmail's, in the same CI step, and the harness has ten new checks: 336, from 292. What stays English on purpose: a settings key's caption and description, which come from the server's OpenAPI document, and the sentence a refused route answered, inside a wrapper that is translated. |
| ✅ | **The Linux package's administration story written down** | **Done 15 September 2026.** The wiki's *Installing on Linux* page had the walk - the database, the ini, the administrator password, the first start under systemd, the Deck - and the README and the page's own *Administering it* section still described the Linux server of 6.3.0, with DKIM, the domain limits, the signature and the relay host named as unreachable. Both say what 6.3.3 reaches: a domain whole over `PUT /api/v1/domains/{domain}` (DKIM signing included), accounts in full, the settings groups, IP ranges, fetch accounts, application passwords, folder permissions and the message store, and the Deck at parity with the desktop program by the census (322 of 330). What is still outside is stated instead of discovered: groups, `Account.ExportMessages`, a GSSAPI bind, a re-keying command. |

## 3. The webmail against Gmail and Outlook.com

What it has, measured against the two on 14 September: a reading pane (right, below, off), conversations, a list with sender, subject, snippet, time and hover actions, search with operators and an options panel, labels with colours, stars, pins, mute, block, sweep, archive, junk, snooze, scheduled send, undo send, templates, signatures and identities, vacation, forwarding, rules, drag-and-drop to folders, right-click menus, keyboard shortcuts and a palette, tabs (Primary, Social, Promotions, Updates, Forums) or Focused and Other, select-all across a folder, attachments with previews and large ones as links, S/MIME, read receipts, one-click unsubscribe, contacts with CardDAV, notifications, offline reading and an outbox, a phone layout that installs as an app, dark theme, twenty languages. What it lacks:

| | Item | Detail |
|---|---|---|
| ⬜ | **The README's screenshots against the real bench** | The tooling is ready (`readme-seed.ps1`, `readme-shots.py`); taken as soon as the webmail gate leaves the build running. |
| ⬜ | **A UX audit on the real bench, desktop and phone, light and dark** | Every view walked with a seeded mailbox and screenshots read, the way the tab bar's overflow was found; what it finds fixed on the same branch. Then the same by a screen reader (NVDA) and by keyboard alone, since accessibility is where Gmail is strongest and most webmails weakest. |
| ⬜ | **Calendar and tasks** | The largest gap; section 4. |
| ⬜ | **Contact groups** | A group in the address book, addressed by name in To, and served over CardDAV as a vCard KIND:group. |
| ⬜ | **Important, and sections of the inbox** | Gmail's importance marker (who you write to, what you open) as a local heuristic with no server learning; an inbox that can show Important, Starred and Everything else as sections. |
| ⬜ | **Delegation in the page** | The server has shared folders and the post right; the page should let an account open a mailbox it is delegated to and send as it, as Outlook's "open another mailbox" does. |
| ⬜ | **Emoji, images inline in compose, tables** | The rich editor takes text, links and formatting; pasting an image, an emoji picker and simple tables are what people reach for next. |
| ⬜ | **Performance at a hundred thousand messages** | Measured, not assumed: the list is chunked, the search bounded; a seeded folder of that size on the bench, timed for open, scroll, search and select-all. |
| ⬜ | **A second account in the same page** | Identities cover addresses of one mailbox; two mailboxes side by side is what Outlook does and Gmail does not. |
| ⏸️ | **Confidential mode, tracking, translation, AI writing, chat and video** | Declined with reasons: an expiring or unforwardable message cannot be enforced for a recipient elsewhere and would be a promise the server cannot keep; tracking readers is the thing this project refuses to do to them; translation and AI writing need a third-party service or a local model, and neither belongs in the server until one can run on the server's own machine; chat and video are not mail. |

## 4. Calendar and tasks (Phase 6 of the client programme)

| | Item | Detail |
|---|---|---|
| ✅ | **CalDAV on the web-services listener** | **In the tree since 15 September 2026 (the batch after 6.3.3), unreleased.** RFC 4791 beside CardDAV, on the WebDAV layer the two now share (`DavSupport`: the bounded XML reader, HTTP Basic through the IMAP logon's lockout, the If-* preconditions, the response builders): `/.well-known/caldav` to this server's own `/dav/`, the principal answering `calendar-home-set`, one calendar per account under `/dav/calendars/<address>/calendar/`, PUT/GET/DELETE with ETags (409 `no-uid-conflict` naming the other object, 403 with the RFC's own precondition names, 415), PROPFIND 0 and 1, REPORT calendar-query (comp-filter, time-range narrowed in SQL and expanded through the module, prop-filter to is-not-defined, text-match and time-range), calendar-multiget (deduplicated, 64 MiB and 507), sync-collection with tombstones; the DAV header `1, 3, addressbook, calendar-access`. Schema 6041: `hm_calendars` and `hm_calendarobjects` (the ics as sent, its span and its first and last instance in UTC, an ETag, a sync token, a tombstone). `POST /api/v1/calendar/expand` expands an iCalendar text over a window for tests and tools. Eleven tests in `RegressionTests.API.CalDav`. Not there: `calendar-timezone` storage, free-busy, partial `calendar-data`, VALARM time-range, MKCALENDAR (one calendar per account). |
| ✅ | **iCalendar** | **In the tree since 15 September 2026, unreleased.** `ICalendar.cpp`: RFC 5545 parsed and written - VEVENT, VTODO, VALARM, VTIMEZONE, folding at 75 octets UTF-8-safe, parameter quoting, the four escapes - bounded (depth 8, 2,000 components, 50,000 properties); DATE, DATE-TIME floating and UTC, TZID through the object's own VTIMEZONE or a built-in table of 592 IANA and Windows names with 2026 rules, gap and overlap hours decided; RRULE DAILY to YEARLY with INTERVAL, COUNT, UNTIL, BYMONTH, BYWEEKNO, BYYEARDAY, BYMONTHDAY, BYDAY with ordinals, BYSETPOS, WKST, expanded in the DTSTART's wall clock across DST; EXDATE, RDATE (DATE, DATE-TIME, PERIOD), RECURRENCE-ID overrides; bounded at 10,000 instances so a hostile rule costs milliseconds. Refused rather than approximated: SECONDLY/MINUTELY/HOURLY, BYHOUR/BYMINUTE/BYSECOND, RANGE=THISANDFUTURE, unknown zones. Rules are today's, not history. The object a client sends is the object it reads back byte for byte except folding. |
| ⬜ | **iMIP: invitations by mail** | REQUEST, REPLY, CANCEL and COUNTER as text/calendar parts; the page shows an invitation card with Accept, Tentative and Decline, replies by mail, and updates the calendar; the server files a REPLY into the organiser's event. The only standards path to Exchange and Microsoft 365. |
| ⬜ | **The calendar page** | Month, week, day and agenda views; create by click and drag, move and resize by drag, a recurrence editor, reminders by notification, time zones, several calendars with colours, shared calendars through CalDAV ACLs. |
| ⬜ | **Tasks** | VTODO lists with due dates and completion, in the page and over CalDAV, which is how Apple Reminders and Tasks.org sync. |

## 5. The Linux port beyond compiling

(The .deb's Boost pin, once a row here, was closed on 11 September: Boost is linked statically and the packages install on Ubuntu 26.04, proven in CI.)

| | Item | Detail |
|---|---|---|
| ⬜ | **The service host: systemd, and a live update on Linux** | The update helper is a Windows program; on Linux the apply step is a package upgrade or an AppImage swap under a systemd unit that restarts the server, with the same outcome file and the same rollback. |
| 🔄 | **The Linux suite's remaining skips** | The anti-virus settings and distribution lists over REST landed 14 September (#260; the hosted run on that tree: 1,493 tests, 1,246 passed, 2 failed, 245 skipped), and the message object, application passwords and folder permissions the same day (#262; the numbers are on the wiki's Changes page, section 4d, written once the batch had landed), each with the shim taught to use it. Left: `Account.ExportMessages`, groups, and the three fixture files that do not yet compile against the shims (`ACL.cs`, `API/Basics.cs`, `MessageUids.cs`). |

## 6. Measured quality

| | Item | Detail |
|---|---|---|
| ⬜ | **Native coverage, measured** | The two silver-badge rows in Roadmap.md: the C++ server has never been measured. A coverage build on the bench, the number published. |
| ⬜ | **The bench, doubled** | One Windows bench runs one gate at a time, about ninety minutes each, and the queue of 14 September was six deep. The Ubuntu VM as a second bench for the Linux suite, in parallel. |
| 🔄 | **A fuzzing job that never stops** | **Written 15 September 2026, first run pending.** `.github/workflows/fuzz-nightly.yml`: every night at 02:10 UTC on a hosted Windows runner, the three MIME harnesses built with the runner's clang-cl, every kept input and committed reproducer replayed first, then each harness for forty minutes with two workers; the corpus kept in the Actions cache under a per-run key so it grows night by night; a finding fails the job and lands as an artifact. Underway until the first scheduled run has come back green; iCalendar and vCard harnesses (the parsers this server now has) are the next targets. |

## 7. What the deep dive of 14 September found missing

Each of these was checked against Roadmap.md before it was written here: none has a row that ships it, and several have a row that says, in so many words, that it does not exist. Grouped by who feels the gap first.

### The administrator

| | Item | Detail |
|---|---|---|
| ✅ | **Alerts** | **Done 15 September 2026.** All six notice-and-tell-nobody conditions now raise an event: a backup that failed, a certificate within the rule's threshold of expiry, the disk at its warning band or below its floor, a work queue whose every thread is stuck, auto-bans past a rate in the last hour, and a minidump newer than the last one recorded. A condition is a row in `hm_alertrules` - name, threshold, on or off, mail and/or webhook, cool-down, digest - so a seventh is a row and never a schema change; the six ship with `backup.failed` and `certificate.expiring` on and the rest off, and nothing is sent until `AlertRecipient` and `AlertSenderAddress` are set. Whatever notices writes a row and returns, and `AlertTask` is the only thing that sends, so a notification about the delivery queue is never submitted into the delivery queue by the thread stuck on it. Four things stop it flapping: a state condition that is already raised is not raised again, an occurrence is bounded by its cool-down, there is a ceiling per hour across the lot, and the daily digest is the quiet default. The webhook body is signed HMAC-SHA256 over `timestamp.body` with the timestamp and signature in headers, retried on the next pass and then at 1, 5, 15 and 60 minutes, and dead-lettered after the last attempt with the event kept. Nine settings in `hm_settings`; five REST routes; `docs/AlertsAndAuditTrail.md` with the verification recipe in Python and JavaScript. **Not done:** a Control Panel page for the rules, and `hmctl` verbs. |
| ⬜ | **Outbound webhooks** | The roadmap row says it plainly: no configurable webhook exists, only an event script an administrator writes by hand. A configured URL per event - message received, delivered, bounced, quarantined, account locked - with a signed body and retries. |
| ⬜ | **An audit trail** | Every administrative change - who, what, when, from where, over COM, REST or a page - in an append-only, hash-chained log the Deck shows. Exchange has it; a regulated customer asks for it first. |
| 🔄 | **Declarative configuration** | **The export, the diff and the apply landed 15 September 2026, in `hmctl config`** (`hmailserver/docs/CommandLine.md`): one JSON document holding the settings groups, the ini keys the server exposes, the domains and their accounts, aliases and distribution lists, the groups, rules, routes, listeners and IP ranges, over the REST API rather than COM - so it works against a Linux server and from a machine that is not the server, which `build/hmconfig.ps1` cannot. The document is sorted throughout and carries nothing the server allocated - no identifier, no count, no time - so two exports of an unchanged server are the same bytes; that is the property a repository needs, and it is checked rather than claimed. `diff` exits 1 when anything would change, so a pipeline gates on it like a failing test; `apply` prints the plan and changes nothing unless `--force`, and a deletion needs `--allow-delete` on top of that, because a document written from one server and applied to another must not quietly remove what the second one has. Twenty checks in `build/check-cli.py` hold all of it against a recorded server, including that an apply without `--force` makes no request that is not a GET. **Left:** certificates and the directories (files and paths of one machine, which would describe a server that does not exist if copied), what lives under an account (application passwords, fetch accounts, folder permissions - the account holder's rather than the configuration's), and the GitOps loop, which is two commands in a workflow and is documented as such rather than built as a daemon. |
| ✅ | **An audit trail** | **Done 15 September 2026.** Every administrative change is one row in `hm_audit`: who (the administrator, the account or `key:<id>`), what (the object, the action, and for a setting the value it had as well as the value it was given), when, from where (the peer address, empty for COM, which has none this process can see) and over which interface - COM, REST or the Deck. Recorded at two places in the whole server, both BELOW the interfaces so that COM and REST are covered by construction: `DatabaseConnectionManager::Execute` for configuration rows and `PropertySet::Set*` for settings. Who is a thread-local actor installed once per interface - the REST dispatcher, and COM's two authorisation questions, which run on the thread that is about to write where one installed at authentication time would not. A password, key, token, secret, hash or salt is recorded as `(changed)` and never as a value, by the name of the column. Each row carries the SHA-256 of the row before it and its own, and `GET /api/v1/audit/verify` walks the chain and names the first row that was edited or the first that points at one removed; retention is understood rather than mistaken for tampering. `GET /api/v1/audit` pages and filters, administrator-only and refused to a key of any scope with a 401; the Deck has an **Audit trail** page in all twenty languages. `build/check-audit-choke-point.py` proves no write route bypasses it - which closed 84 COM methods that persisted something without saying who. |
| ⬜ | **Declarative configuration** | Export the whole configuration - settings, domains, accounts, rules, routes, certificates, listeners - as one file; apply one with a diff shown first; a GitOps loop that applies a repository on change. Stalwart's file and Postfix's main.cf are what people compare against, and the REST write surface is most of the work already done. |
| ⬜ | **The reports our own domains receive, read** | The server sends DMARC and TLS-RPT reports; it does not read the ones that come back for its own domains. A mailbox address per domain, the aggregate reports parsed, and a Deck page that charts who sends as the domain, with what result, the way dmarcian does for a fee. |
| ⬜ | **Own-reputation watch** | The server's outbound addresses checked against the major DNSBLs once a day, and an alert when one lists them - the first thing an administrator learns otherwise is a bounce. |
| ⬜ | **Compromised-account detection** | Per-account sending limits exist. What is missing is the shape of an account takeover: a sudden change in volume, recipients, hour or client address, which locks the account, keeps the mail in the queue for review and alerts. |
| ⬜ | **Outbound content policy (DLP)** | Rules on what leaves: card and account numbers by pattern, attachment types, size, an external recipient on a message marked internal - block, quarantine for review or warn the sender. Exchange has it; no open server does it well. |
| ⬜ | **Quarantine digests for users** | A daily message to each account whose quarantine holds something new, with one-click release from the webmail's held-mail page. |
| ⬜ | **PST import** | Every migration from Exchange and Outlook has a PST somewhere. A reader for the PST format in the import tool, so a migration is drag, wait, done. |
| ⬜ | **Event scripts on Linux** | The Windows build hosts VBScript and JScript through Active Scripting; the Linux build has no engine, and the roadmap's Linux row skips every scripting test for it. One cross-platform engine - JavaScript through an embeddable interpreter - for the same events on both, with the Windows engines kept. |
| ⬜ | **Passkeys and single sign-on** | Passkeys (WebAuthn) for the webmail and Deck sign-in with TOTP as the fallback; OIDC sign-in so an organisation's identity provider is the front door. The server already validates bearer tokens for SMTP and IMAP; the pages should take the same identity. |
| ⬜ | **A password checked against known breaches** | At every password change, the k-anonymity check against the breach corpus (five characters of the hash leave the server, nothing else), refused when found. |
| ⬜ | **Spam traps** | Addresses that were never real; a message to one auto-bans the sender and trains the score. |

### The organisation

| | Item | Detail |
|---|---|---|
| ⬜ | **Active-active nodes** | The warm-standby topology is documented and tested; what remains is two or more nodes serving the same domains at once - shared database, shared or replicated message store, queue ownership by node, sessions on any node - so a node can be taken down without a failover. |
| ⬜ | **Object storage for the message store** | Messages on S3-compatible or Azure Blob storage with the database as the index, which is how a store grows past one disk and how the active-active row shares a store. |
| ⬜ | **Mailbox encryption at rest** | A key per account, the message files encrypted with it, the key unlocked by the password at sign-in and by a recovery key held by the administrator - Dovecot's mail-crypt shape. The search index has to be built with the same care or it leaks what the files hide. |
| ⬜ | **Storage compression and attachment de-duplication** | Messages compressed on the way to disk and the same attachment stored once, measured on a real mailbox before it is claimed. |
| ⬜ | **A list manager** | Distribution lists with moderation exist. A list a person subscribes to by mail and by page, with an archive, digests, per-member delivery settings and the List-* headers filled in, is what replaces Mailman for a small organisation. |
| ⬜ | **Message recall within the server** | An unread message sent to accounts on this server can be withdrawn by its sender, which Outlook does within Exchange and nothing does across the internet. |

### The person reading mail

| | Item | Detail |
|---|---|---|
| ⬜ | **Web Push** | Notifications while the page is open exist. Web Push through the browser's push service, with the server holding the subscriptions, means new mail is announced when the installed app is closed, which is the difference between a page and an app. |
| ✅ | **Saved searches as folders** | **Done 15 September 2026.** The search options panel takes a name and *Save search*: the search is kept in the account's preferences (`saved_searches`, twenty at most), listed in the folder navigation under *Saved searches*, and opening one runs it again with its name as the title - always current. The panel lists them with a remove. Three harness checks. |
| ⬜ | **Manage subscriptions** | One page listing every sender that offers an unsubscribe, with how much it sends and one button per row - Gmail's, and the reason most people go looking for it. |
| ⬜ | **Brand logos** | BIMI on receipt: the logo a sender publishes, verified, as the avatar in the list, which Gmail and Outlook.com both show. |
| ✅ | **Contacts in and out** | **Done 15 September 2026.** The Contacts page exports the address book as vCard 3.0 (one card per contact: FN, N, EMAIL) or as CSV with the `Name,E-mail Address` header Outlook reads, and imports either: a vCard file of any number of cards (folded lines, escapes, several EMAILs on one card, quoted-printable cards skipped) or a CSV read by its own header row - Google Contacts' `Name` and `E-mail 1 - Value`, Outlook's `First Name`, `Last Name` and `E-mail Address`, or a plain `name,email` - adding what is not there yet by address and saying how many came in. The wiki's client page walks the Google and Outlook exports. Five harness checks. |
| ⬜ | **Snooze, scheduled send and reminders on the phone** | The same features exist; whether they are reachable and readable on a phone is what the UX audit in section 3 measures. |

### The project

| | Item | Detail |
|---|---|---|
| ⬜ | **Published numbers** | Messages per second in and out, IMAP sessions per core, memory per session, on named hardware, against Postfix and Dovecot on the same machine, with the load generator in the repository so anyone can repeat it. The claim in this file's first line is not one until this row is done. |
| ⬜ | **Windows on ARM** | The Linux port builds for AArch64; the Windows build does not. Visual Studio's ARM64 tools and the same installer. |
| ⬜ | **macOS** | The POSIX port should build on macOS with little work; a Homebrew formula makes it a developer's local server. |
| ✅ | **A Helm chart and a compose file** | **Done 15 September 2026.** `platform/packaging/docker/docker-compose.yml` (the server beside PostgreSQL, secrets from files, the REST listener over TLS from a mounted certificate, the image's own health check) and `platform/packaging/helm/hmailserver` (a StatefulSet of one with its three volumes, a LoadBalancer with the mail ports and the REST port, the two passwords as a Secret or an existing one, the REST certificate from a `kubernetes.io/tls` Secret, probes on port 25 or on the metrics listener's `/livez` and `/readyz`; the database pointed at, never bundled). Neither tool is on the bench, so `.github/workflows/packaging.yml` lints and renders the chart and resolves the Compose file on every change. |

## 8. Declined in the deep dive, with reasons

| | Item | Why not |
|---|---|---|
| ⏸️ | **Apple push for iOS Mail** | Dovecot's XAPPLEPUSHSERVICE needs a certificate only Apple's own server program is issued; without it the extension is a promise the server cannot keep. IMAP IDLE and the installed webmail's Web Push are the honest answers. |
| ⏸️ | **Publishing BIMI for our domains** | It is DNS and a Verified Mark Certificate that costs a sender money each year; the server can document the record but there is nothing to ship. |
| ⏸️ | **A Bayesian classifier of our own** | Deferred in Roadmap.md and still right: rspamd and SpamAssassin are integrated and learn; a second learner in the server would be worse than both. |

## 9. End-to-end encryption

Transport is done: TLS everywhere, DANE, MTA-STS, a post-quantum key exchange, and S/MIME in the page since 11 September. What is not done is mail that stays unreadable to the server itself, and to anyone who takes the server. Proton and Tuta built businesses on that; theirs is proprietary in the sense that only their client reads their format. This server's version is built on OpenPGP, so that any client with a key reads the mail and no one is locked in - the standard is the feature. Roadmap.md deferred OpenPGP in the browser on 11 September as "the S/MIME page's design on another format"; it is un-deferred here, as the first row of the four.

| | Item | Detail |
|---|---|---|
| ⬜ | **OpenPGP in the page** | The S/MIME module's shape - keys made or imported in the browser, kept encrypted under a passphrase, unlocked for a session - for OpenPGP: sign, encrypt, decrypt, verify, with the lock and the signature badge the S/MIME messages already show; a key page with fingerprints, expiry and revocation; Autocrypt headers on outgoing mail so a correspondent's client learns the key, and Autocrypt read on incoming; keys of correspondents looked up by WKD and, where a domain has none, by a keyserver. Interoperates with Thunderbird, GnuPG, Mailvelope, Proton and every client that speaks the standard. |
| ⬜ | **The server publishes its users' keys** | Web Key Directory for every domain the server hosts: `/.well-known/openpgpkey/` on the web-services listener, advanced method, so that any client on the internet finds a user's key from the address alone. The keys are the ones made in the page or uploaded by an administrator for a client that keeps its own. |
| ⬜ | **Zero-access mailboxes** | Opt-in per account, the Proton model: after the filters have run - spam, virus, rules, which need the text - a delivered message is encrypted to the account's public key and the plaintext never touches the disk; the private key is encrypted under a key derived from the password with Argon2id and unlocked in the browser, and a recovery key the user writes down is the only other way in. The administrator cannot read the mail, cannot search it, and cannot recover it. Written down as the trade-offs it is: search runs over the client's own index or the headers the server kept; IMAP and POP see ciphertext, so an account with this on is a webmail account until a local bridge exists; a forgotten password without the recovery key is the mail lost, and the page says so before it is switched on. |
| ⬜ | **A password-protected message to anyone** | For the correspondent with no key: the sender sets a password, the page encrypts the body and attachments (OpenPGP, symmetric) and sends a short message with a link to a reading page on this server; the recipient enters the password there and the browser decrypts; a reply written on that page goes back the same way. End to end, because the plaintext is never in the message that crosses the internet and never on this server; unlike "confidential mode", it promises only what it can keep. |
| ⬜ | **Trust made visible** | A correspondent's key is remembered the first time it is seen and a change is a warning in the reader, not a silent switch - the one thing a man in the middle needs is silence. Fingerprints shown, verifiable by voice or in person, and a green mark on a verified correspondent from then on. |

## 10. The second sweep: product by product, feature by feature

Gmail and Google Workspace, Outlook.com and Microsoft 365 with Exchange behind it, Proton Mail, Fastmail, Tuta, Zoho, Zimbra, Stalwart, mailcow and iRedMail, MDaemon, Kerio and Axigen, each walked through its own feature list and settings pages, and every item below checked against Roadmap.md and the page's own script before it was written down. Where a name is given in brackets it is the product that people know the feature from. Sorted by who feels the gap.

### Normal users: reading and writing mail

| | Item | Detail |
|---|---|---|
| ⬜ | **A message that expires** (Gmail's Confidential Mode) | The sender sets an expiry - an hour, a day, a week - and the message stops being readable when it passes. The body and the attachments are kept on this server rather than sent: what crosses the internet is a short notice with a link, the reader signs in (an account here, or a one-time code sent to the address, or a passcode the sender gives them another way), and the page shows the message with no download, no forward and no copy button, and no print stylesheet. The sender can revoke it before it expires, and sees when it was first opened. **What it is not, said as plainly in the product as it is here:** a reader who can see a message can photograph it, and a reader signed in on this server could save the page. This stops a message being forwarded on by accident or found in a mailbox a year later; it does not stop a determined recipient, and a product that implies otherwise is lying to the sender. Where the recipient is on this server, the same expiry can be enforced in their mailbox rather than through a link. It builds on [the password-protected message to anyone] in section 9 - the same reading page, the same short notice - and differs in who holds the key: that row is end to end and this one is not, because a message this server can delete on a timer is a message this server can read. Both are worth having, and each should say which it is. |
| ✅ | **Attachment reminder** (Gmail, Outlook) | **Landed 14 September (#259).** "attached", "see the attachment", "enclosed" in the body and nothing attached: one question before it sends. The compose form has the words in front of it already; it only has to read them. |
| ✅ | **@mentions in the body** (Outlook) | **Done 15 September 2026, in the plain text editor.** An @ followed by letters at the caret asks the address book as the To field does; the person chosen is written as @Name where the @ was and added to To if not there; a message whose subject or first line mentions the reader - @ and their first name, or the mailbox's name - carries an @ mark in the list. The name is text, not a link: a plain-text message has no links, and the formatted editor is not taught this yet. Two harness checks. |
| ✅ | **Shift-click and Ctrl-click in the list** (every desktop client) | **Landed 14 September (#259).** The list has one way to select several rows - the boxes, one at a time. Shift-click for a range, Ctrl-click to add one, as the folder page and every file manager does. Checked: the script has no shift-key handling at all. |
| ✅ | **Nudges** (Gmail) | **Landed 14 September (#262):** "Received N days ago. Reply?" from the listing's own fields, and "Sent N days ago. Follow up?" on a sent message nothing answered, asked of the server once per Sent view through the new `in_reply_to:` operator; a `nudges` preference turns them off. The row as written: "Sent 3 days ago, no reply" on a message the reader sent that got none, and "received 3 days ago, not answered" on one that asked a question - a heuristic on dates and the answered flag, no learning. |
| ✅ | **Follow-up flags with a date, and a reminder** (Outlook) | **Landed 14 September (#262):** the star plus `$FollowUp` and a `$Due-YYYY-MM-DD` keyword on the message itself (the flags route takes any IMAP atom, so other clients still see a flag and a move keeps the date), today, tomorrow, next week or a date from the toolbar, a due badge on the row, the Starred view sorted soonest first, and a reminder once a day at sign-in and when the day turns. The row as written: A flag is on or off today. A flag with a due date, a Flagged view sorted by it, and a notification when it comes due; the date is the `$FollowUp` keyword plus a date the page keeps, so other clients still see a flag. |
| ✅ | **Quick steps** (Outlook) | **Landed 14 September (#262):** defined on a Settings card and kept in the preferences (`qs.<slug>`), as buttons over the list and on the digit keys 1 to 9; move, mark read, label, forward, in one press. The row as written: One button the reader defines: move to a folder, mark read, label, forward to an address, in one press, with a keyboard shortcut of its own. |
| ✅ | **Clean up conversation** (Outlook) | **Landed 14 September (#262):** from the row's menu and the message's, deleting the messages whose every line is quoted in a later message of the conversation - the newest, the unread, the starred and those with attachments kept - with Undo. The row as written: Delete the messages of a conversation whose whole text is quoted in a later one, so a thread of twenty is the three that said something. |
| ✅ | **Message pop-out** (Outlook, Gmail) | **Landed 14 September (#259).** Open a message, or the compose form, in its own window, so a person can read one while writing another. |
| ✅ | **Search operators the two have and this one lacks** | **All thirteen landed 14 September** - `cc:`, `bcc:`, `filename:`, `larger:`, `smaller:`, `older_than:`, `newer_than:`, `is:muted`, `is:pinned`, `category:`, `-word` and `OR` (#259), then `has:link` (#262: an `http` or `https` address in the text, or an `href` in the HTML, so a remote image alone is not a link) - each with a test; and `in_reply_to:<message-id>` besides, which the nudges ask. The row as written: `cc:`, `bcc:`, `filename:`, `larger:10M`, `smaller:`, `older_than:7d`, `newer_than:`, `is:muted`, `is:pinned`, `category:promotions`, `has:link`, `-word` to exclude, `OR`. The operators today: from, to, subject, has:attachment, before, after, in, is:unread/read/flagged/unflagged/answered, label. Each new one is a line in the server's search and a line in the options panel. |
| ✅ | **Search history and suggestions** (Gmail) | **Landed 14 September** - the history (#259), then a contact's name, or `from:`/`to:` and a name, completing to `from:<address>` under the box from the address book (#262). The row as written: The last searches offered under the box as it is focused, and a contact's name completing to `from:` as it is typed. |
| ✅ | **Saved searches** | Done 15 September 2026; section 7. |
| ⬜ | **Recover deleted items** (Outlook) | **The premise as first written was wrong, checked 14 September:** the IMAP expunge retention task caps `hm_imapexpunged` - folder, UID and mod-sequence, for QRESYNC - and an expunge deletes the file; nothing retains a message. So this is a store first: a retention window (a setting, default off) under which an expunged message's row and file move to a recoverable state instead of going, a sweep that ends them after the window, a `/api/v1/me` pair to list and restore, and then the Recover page. A schema change, and the IMAP expunge path; sized as a wave of its own. |
| ⬜ | **Auto-delete and auto-archive the reader chooses** (Proton, Outlook) | "Delete Trash and Junk after 30 days", "archive Inbox mail older than a year" as the reader's own settings, run by the server's retention task; today retention is the administrator's, per domain and account. |
| ⬜ | **Masked addresses** (Fastmail's Masked Email, Proton's hide-my-email, Apple's Hide My Email) | An alias the reader makes in a click for one shop or one sign-up, named for it, forwarding to the mailbox, with a reply from the mailbox rewritten to come from the alias, and a switch that turns it off the day it starts to receive spam. The server's aliases are the administrator's; these are the reader's own, bounded per account. |
| ⬜ | **Other accounts, fetched** (Gmail's "check mail from other accounts") | The server fetches external POP3 and IMAP accounts for an account; only an administrator can set one up. A page in the webmail where the reader adds their old provider's account and sees its mail arrive here. |
| ⬜ | **Send-as an address that is verified** (Gmail) | An identity is what an administrator gave. A reader should be able to add an address they own elsewhere, prove it with a code sent to it, and send as it from then on. |
| ✅ | **Signatures with images and layout** (Outlook, Gmail) | **Done 15 September 2026, for the account's signature.** Settings has a formatted signature beside the plain one: bold, italic, underline, a link, and an image from a file kept as a data URI (PNG, JPEG, GIF or WebP, 200 KB at most), cleaned by the same cleaner the editor uses and saved as `signature.html`, which the server had stored all along; the formatted signature goes on formatted messages, replacing the plain block when formatting is switched on, and the plain one stays on plain messages. Per-identity signatures are the identities' own row. Two harness checks. |
| ✅ | **Trackers, counted** (Proton, Apple Mail) | **Done 15 September 2026.** The notice that remote images were not loaded says how many, from how many hosts, and how many look like tracking pixels (a pixel or less, hidden, or named as one - pixel, beacon, open, track); and the sender's habit, the number of their messages seen carrying pixels, kept in this browser and shown on the notice. Two harness checks. |
| ✅ | **Report phishing** (Gmail, Outlook) | **Done 15 September 2026, in the page.** *Report phishing* in the message's menu sends the message's source to the postmaster of the reader's domain as an attachment (with what the page knows of it in the text), files the message as junk, which teaches the filter, and blocks the sender with the rule *Block this sender* writes, whose toast carries the Undo. A URL list for the SURBL-style check is the server's side and stays a row. Three harness checks. |
| ⬜ | **Safe senders** (Outlook) | A reader's own allow list - never junk from this address or domain - fed to the spam filter, beside the block list that exists. |
| ✅ | **Conditional formatting of the list** (Outlook) | **Done 15 September 2026.** *Colour rows* on the Filters page: a field (from, to or cc, subject, any), a text and a colour, kept with the account's preferences (twenty at most); the first rule that matches paints the row's edge, in the list and on conversation cards. Four harness checks. |
| ✅ | **Multiple stars** (Gmail) | **Done 15 September 2026.** The star pressed again cycles: the star, a second in amber, a third in red, then none - the IMAP flag every client shows, with the keywords `$Star2` and `$Star3` on top of it that other clients ignore; the Starred view holds all three. In the list and on the open message. One harness check over the four presses. |
| ⬜ | **The star key means two things** | Found 15 September 2026 while the shortcuts document was being checked against the page. `s` on a row in the list cycles the three stars, which is what the feature is; `s` on an open message toggles the IMAP flag alone, so a message opened can never be given the second or third star from the keyboard, and a third-starred message toggled off keeps its `$Star3` keyword with the flag cleared - invisible in this page, and untidy in any client that shows keywords. One key, one meaning: the open message should cycle as the row does, and clearing the star should clear the keywords with it. Small, and with a harness check to write beside it. |
| ✅ | **Snooze presets and custom swipe actions** (Gmail, Proton) | **Done 15 September 2026.** Settings: what a swipe to the right and to the left does on a touch screen (archive, delete, junk, mark as read, nothing), the hours of the first snooze preset, and the hour of the day for *tomorrow* and *next week* - kept with the account, the snooze menu's captions following. Four harness checks. |
| ⬜ | **A mailbox export** (Google Takeout, Proton's export) | The reader downloads their whole mailbox as mbox and their contacts as vCard, from Settings, with no administrator involved - data portability as the law and decency require. |
| ⬜ | **Import from the old provider** (Proton's Easy Switch, Fastmail's import) | The reader's side of the migration: sign in to Gmail or Outlook.com over IMAP (OAuth for the two), pick folders, and watch them arrive - the server's IMAP mirror, driven from the page for one account. |
| ✅ | **Right-to-left languages** | **Done 15 September 2026.** Arabic, Hebrew and Persian - `ar.json`, `he.json`, `fa.json`, 631 keys each, the full page - take the catalogues to twenty-three, and the page is mirrored for them. The direction is decided in the one place every language change passes through, whichever way it arrived (the server's `Accept-Language` block in the page, the choice this browser remembers, the account's preference): `applyCatalogue` writes `lang` and `dir` on the root element. The stylesheet does the rest, because it was rewritten in logical properties - `margin-inline-start`, `padding-inline`, `inset-inline`, `border-inline-end`, the four logical corner radii, `text-align: start` - so one sheet serves both directions and every computed value in English is what it was. A message is exempt and carries `dir="auto"`: the text pane, the HTML frame, a conversation card's body, the subject field, the textarea and the rich editor each settle their own direction from their own first strong character, which is the only right answer when an Arabic reader opens an English message. Four things needed an explicit rule and each says why in a comment: the *sign* of an inset `box-shadow`, which has no logical form (the row cursor's bar and a colour rule's bar, through a `--lead` custom property that is 1 or -1); fourteen direction-bearing icons flipped about their view box; the narrow-screen drawer, parked on the other side; and the context menu, which is placed in viewport coordinates and now opens away from the pointer. Deliberately left physical, and commented: the tooltip and the full-screen compose window, both centred and direction-neutral, and the swipe gestures, whose left and right are the reader's fingers. The harness gained ten checks - 274, from 264 - and its own defect was found on the way: its DOM's text nodes had no `nodeType` and no `nodeValue`, so `localiseTree` walked past every one of them and all 264 checks before this had been asserting the English page. The first paint is in the reader's direction too: the server writes the page's `<html>` tag for the catalogue it has just negotiated, so the frame that arrives is already the right way round, and the script writes the same two attributes again when it runs and on every later change. |
| ✅ | **The reader's own time zone** | **Done 15 September 2026.** Settings has a *Time zone* choice - the zones the browser knows, *Where this browser is* as the default - kept with the account; the list's times, the open message's date and the conversation cards render in it, and today and this year are decided in it. Three harness checks. |
| ✅ | **Undo more** | **Done 15 September 2026.** The toast with Undo that filing and sending have, for the three that lacked it: a label taken off a message (Undo puts it back on that message), the rules saved by an addition or a removal (Undo writes the script back as it was), and a contact removed (Undo adds it back). Four harness checks. |

### Normal users: the phone

| | Item | Detail |
|---|---|---|
| ✅ | **The app badge** (every mail app) | **Done 15 September 2026.** `navigator.setAppBadge` with the same total the title carries - the folders the reader asked to be told about - on every count the page reads, cleared at zero; nothing without the API. |
| ✅ | **Share to the webmail** (every mail app) | **Done 15 September 2026.** The manifest declares a share target (`POST /portal/share`, multipart: title, text, url, files - images, PDFs, text, calendars, `.eml`); the service worker answers the POST by putting what was shared into a cache of its own and sends the page to a new message, which reads the cache once, fills the subject and the text, attaches the files as if dropped, and deletes it. |
| ✅ | **The mailto: handler** | **Done 15 September 2026.** Settings offers *Open mailto: links here*, which calls `registerProtocolHandler` for `/portal#/compose?mailto=%s` (the manifest declares the same); the compose route parses the mailto: URL - addresses, subject, body, cc, bcc - into a new message. |
| ⬜ | **Offline for the last thirty days** (Gmail offline) | Offline holds the last listing and an outbox. The last thirty days of bodies and attachments kept in the browser's storage, searched offline, bounded by a setting. |
| ⬜ | **Web Push** | Section 7. |

### Normal users: contacts and the organisation

| | Item | Detail |
|---|---|---|
| ⬜ | **The organisation's directory** (Outlook's Global Address List, Google's directory) | Every account and list of the domain in the completion and on a directory page, with a photo, a title and a phone number the administrator or the person sets, and an opt-out per account. Today the completion knows only the reader's own contacts. |
| ⬜ | **Collected addresses** (Gmail's "Other contacts", Outlook's suggested recipients) | Everyone the reader has written to, offered in completion without being a contact, promoted to one in a click. |
| ⬜ | **Contact photos, birthdays, notes and custom fields** | The contact is name and addresses. A photo (shown as the avatar), a birthday (shown on the day), notes and the vCard's other fields, in the page and over CardDAV. |
| ⬜ | **Shared address books** (Zimbra, Kerio) | An address book shared by a team, read-only or writable, over CardDAV too. |

### Administrators: day to day

| | Item | Detail |
|---|---|---|
| ✅ | **Accounts from a spreadsheet** (every hosted admin console) | **Done 15 September 2026, in the command-line clients** (the row above): `hmctl accounts import people.csv --dry-run` says what would happen and changes nothing, `hmctl accounts import people.csv` makes them, `hmctl accounts export example.com accounts.csv` writes them back out, and `Import-HmAccount -WhatIf` / `Export-HmAccount` are the same in PowerShell. The header row names the fields and `address` is the only one required - anything the account resource takes may be a column, so the file needs no change when the account gains a field - an empty cell is not sent, so the server's own default applies, and **an address that already exists is left alone and counted, never overwritten**, because an import that silently reset a password would lock a hundred people out with one command. The export writes no password column: the server does not have them to give, and a column of blanks re-imported would be a column of accounts nobody can sign in to. What is still open is the same thing from a page rather than a shell - a file dropped on the Deck or the Control Panel - which is worth doing when the Deck's shape half is. |
| 🔄 | **Reports** (Google Workspace, Microsoft 365, Axigen) | **Built 15 September 2026; two of the seven numbers are still only server-wide.** `GET /api/v1/reports` is an index of what can be reported and, as plainly, what cannot; `GET /api/v1/reports/<section>` answers one of seven for a window (`from`, `to`, 366 days at most), optionally one domain, and `format=csv` writes the same columns and rows as `text/csv`. A Control Deck page reads them all in one request with a date range, a domain picker, a bar chart per day and a CSV link on every table; `hmctl report <section> --from --to --domain --csv` is the same from a shell. **Where the numbers come from decides what they can say.** Messages in and out, the failures by SMTP status code with the reason in words, and the top senders and recipients are counted from the message trace (schema 6020) in one grouped query over the window, so the cost is distinct (day, event, status, sender, recipient) tuples rather than deliveries - but the trace is **off by default** (`MessageTraceEnabled`), records only deliveries and failures, and has no spam or virus event in it. The largest mailboxes and the per-domain roll-up are one grouped pass over `hm_messages`, and are the size **now**: nothing has ever recorded what a mailbox held yesterday. Storage growth is answerable at all only because this row added two gauges to the metric sampler - `store_bytes` and `store_messages`, recomputed at most once an hour and served by the Prometheus exporter and the metric history alike - so the series starts the day this ships. Only local domains are counted, and a domain-restricted key sees its own and is refused the two server-wide sections outright, as it is refused the delivery queue. **What is still open, and is the reason this row is underway rather than done:** spam and virus counts per domain, and storage growth per domain. Both counters are server-wide with no domain in them; attributing either honestly needs a per-domain daily sample, which is a schema step and a new table. The report says so in its own index rather than extrapolating. |
| ⬜ | **Follow one connection live** (Stalwart's tracing) | A session id in the Deck, and every line the server logs for it as it happens - the debugging that a grep through a gigabyte of log stands in for today. |
| ✅ | **syslog** (every Linux administrator's first question) | **Done 15 September 2026.** A copy of every log entry, as RFC 5424, beside the log files rather than instead of them - hooked to `Logger::Write_`, the same chokepoint the OTLP log exporter uses, so it exports exactly what the log mask already produces and an ERROR reaches a collector even when logging is off. `SyslogFormatter` renders the header (priority from facility and severity, version, an RFC 3339 timestamp with a real offset taken from the entry rather than from a second clock reading, hostname, `hMailServer`, the process id, and the log category as the message id - or the `ErrorManager` number, `HM5165`, on an error line that carries one), the structured data where it says something (`thread`, and `session` and `client` on a conversation line, under `hmailserver@32473`, which is the enterprise number IANA reserves for examples because this project has none), and the UTF-8 message behind the byte-order mark RFC 5424 asks for. `SyslogSink` is the transport: UDP truncated to 1024 bytes on a character boundary and never into the header, TCP and TCP over TLS (RFC 5425) octet-counted and truncating nothing, TLS verified by the server's own `CertificateVerifier` against the machine's root store with the host name checked. On Linux under systemd with no collector host configured it writes to the journal over `/run/systemd/journal/socket` in the native protocol, with the same fields plus `HMAILSERVER_SESSION`, `HMAILSERVER_CLIENT`, `HMAILSERVER_CATEGORY` and `HMAILSERVER_MSGID`, so `journalctl -u hmailserver HMAILSERVER_SESSION=41` follows one conversation. **Nothing here can delay or lose a message:** the entry is copied on the logging thread into a queue of 4096 that drops the oldest, every socket is on a worker thread of its own, a stream that will not connect backs off to a minute and is not retried while the back-off holds, and the outage is reported once at the start and once at the end rather than once per failure. Seven settings in `hm_settings` - not in `hMailServer.ini`, per the rule of the same day - with a Control Panel card, twelve COM properties on `IInterfaceLogging` and twelve REST keys under `/api/v1/settings/logging`, which is how it is turned on on Linux, where there is no Control Panel. `RegressionTests/Infrastructure/SyslogExport.cs` stands its own UDP listener up and asserts the header field by field on a real datagram, the facility and severity arithmetic for `local0` with a negative control, the structured data, the byte-order mark, that no datagram exceeds 1024 bytes and that the cut falls on a character boundary, that a category switched off is not sent while one switched on is, and that five deliveries to a collector refusing every connection still arrive and still arrive quickly. `hmailserver/docs/Syslog.md` is the walk. |
| ⬜ | **Country blocking** (MDaemon's location screening, every firewall) | Connections and sign-ins by country, from a GeoIP database the administrator supplies, allowed and denied per listener, and the country on every sign-in line and session. |
| 🔄 | **`winget install hMailServer`** (and Chocolatey) | **Built and checked on every release since 15 September 2026; not yet in the public repositories.** Three winget manifests and a Chocolatey package as templates under `hmailserver/installation`, rendered by `build/make-package-manifests.ps1` from the release's own signed installer - downloaded, its Authenticode signature required, and the SHA-256 computed from those bytes, because the signed installer is not the built one and a hash copied between two windows is a broken install for every user of it (6.3.3: `45A9D7F1…` as built, `3188BE2F…` as published). Every identity field is the installer's own - `MinVersion` as the minimum OS, `AppId` as the `hMailServer_is1` product code that lets `winget upgrade` recognise an installation winget did not make, `AppVersion` as the version it compares - and the silent switches are the smoke test's, less `/TYPE=full`, which on an upgrade would put the Control Panel back on a server that had left it out. `build/check-package-manifests.py` validates them against Microsoft's published schemas and holds the two packages to the same URL, hash and version; the `Package managers` workflow renders, checks and packs on `release: published` and on every pull request that touches a template, and submits only when a repository variable and the tokens are set. Verified on 6.3.3: `winget validate` accepted the manifests, the schema check passed, `choco pack` built the package. **What is left is not ours:** the first submission of a new identifier is reviewed by a person at microsoft/winget-pkgs and by Chocolatey's moderation, so the row stays underway until those two have merged. Neither identifier is taken - `winget search hmailserver` finds nothing and the community feed has no `hmailserver`. `hmailserver/docs/PackageManagers.md` is the walk. |
| ✅ | **A PowerShell module and a Linux command-line client** | **Done 15 September 2026.** Two clients with one vocabulary over the REST API, so both work against a Windows server, a Linux server or one on another machine, and neither needs COM: `hmailserver/source/Tools/Cli/hmctl`, one Python 3 file with nothing but the standard library behind it, which the Linux packages install as `/usr/bin/hmctl` (python3 recommended, not required), and `HmailServer.psm1` beside it - `Get-HmDomain`, `New-HmAccount`, `Set-HmSetting` and the rest, answering objects rather than text, every changing verb supporting `-WhatIf` and `-Confirm`, a domain or an address completing from the server itself. Domains, accounts, aliases, distribution lists, groups and their members, rules, routes, listeners, IP ranges, certificates, fetch accounts, API keys, the queue, held mail, the nine settings groups, the ini keys, the logs, the backup, an account's application passwords, the rule-criterion probe, and `hmctl api` for a route newer than the file. Exit codes a script can read: 3 when the credential was refused, 4 when what was named does not exist. **They are held together by a check rather than by intention:** `build/check-cli.py` stands a recorded server in front of both and asserts the method, the path, the Authorization header and the body of every request each one makes - 323 checks, in CI - because what goes wrong in a client like this is the vocabulary pointing at a route that is not there, and a check needing a running server is a check nobody runs. It found four defects while it was being written, two of them in what a person would have called finished: `hmctl` refused `--json` typed after the verb, which is where it is typed; its bulk verbs read accounts from `/api/v1/accounts?domain=`, which is not a route (they are listed under their domain), and the 404 was being swallowed so every address looked new; the two clients disagreed about whether to send `active` on a create, and neither should invent a field the caller did not give; and the password prompt asked `isatty()`, which on Windows answers **true for the NUL device** - so an unattended run with stdin redirected would have sat at a prompt nobody could see until something killed it. `hmailserver/docs/CommandLine.md` is the walk. |
| ⬜ | **Restore one mailbox, or one message, from a backup** (every hosted service) | The backup restores the whole server. An administrator restoring one person's mailbox as it was last Tuesday, or one deleted message, from the Deck. |
| ⬜ | **Mailbox rename and move** | An address renamed with the old one kept as an alias and mail redirected; a mailbox moved between domains on the same server. |

### Enterprise: policy, compliance and identity

| | Item | Detail |
|---|---|---|
| ✅ | **External-sender tagging and first-contact tips** (Microsoft 365, Google) | **Done 15 September 2026.** Per domain, off by default, and while they are off nothing is read, queried or written. Three switches: `[EXTERNAL]` at the front of the subject (the text is the domain's own, at most 100 characters, so a domain whose readers do not read English can say it in the language they do); `X-hMailServer-External`, which the webmail turns into a line above the message and which - unlike the subject - costs the sender's DKIM signature nothing, because prepending a field is signature-safe and rewriting the Subject is not; and the first-contact note, stamped when an outside sender writes to an account that has had mail from other people but never from them. **The rule for "outside" is one function with a unit test** (`MessageOrigin::IsExternalSender`, run from `ClassTester::DoTests`): not authenticated, not through a trusted incoming relay, and neither the envelope sender nor the From address at a domain this server hosts - *either* of the last two being outside is enough, so a forged internal From on external mail is still tagged. The tag goes on **the recipient account's own copy** at local delivery, because the switch belongs to the recipient's domain and a message addressed both here and outside must not carry the tag out with it. The first-contact memory is `hm_knownsenders` (schema 6044), one row per account and sender: two database round trips per delivered copy with the switch on, none with it off, and the account's own outgoing recipients are recorded too, so a reply is never announced as a first contact. **What it deliberately does not do:** it is not anti-spoofing - a message forging a hosted address in both its envelope and its From is judged inside and not tagged, which is what DMARC is for - and an installation that lists its inbound filtering gateway as an incoming relay will see nothing tagged, because that list is a statement that the hop is its own. Both are said plainly in `hmailserver/docs/DomainTransforms.md`. |
| ⬜ | **Impersonation protection** (Microsoft 365's anti-phishing policies) | A display name matching an account or a protected person on mail from outside; a sender domain one edit from ours or from a protected partner (homoglyphs included); quarantined or banner-flagged, per domain. |
| ⬜ | **Mail flow rules** (Exchange transport rules, Google's routing) | Conditions on sender, recipient, headers, size, attachment types, words, and actions: add a disclaimer, stamp a header, redirect, BCC to an address, reject with a text, require TLS, quarantine. The rules today act for an account; these act for the server and the domain, before delivery. |
| ✅ | **A domain disclaimer** (every business) | **Done 15 September 2026.** Per domain, off by default, text and HTML - one box filled in is used for both, with its line breaks turned into line breaks in HTML. It runs where the domain signature runs, on the same `MessageData` and committed by the same single write, because that is the one place on this server where an outgoing body is modified; that is also **before** `DKIMSigner::Sign`, so this server's own signature covers the footer rather than being broken by it. Appended in place to the `text/plain` and `text/html` parts wherever they sit in a multipart tree. **Once, not once per hop:** a reply that quotes a message already carrying the footer gets no second copy, compared after quote markers, tags and white space are removed from both sides, so the `>` prefixes a client adds do not hide the match. **Never** on a signed or encrypted message - `multipart/signed`, `multipart/encrypted`, the PKCS#7 and PGP types - and what happens instead is that the message goes out exactly as written with a line in the application log naming it, because appending to a signed part breaks verification in every client that checks and an encrypted one cannot be read at all. Never on an auto-reply, a bounce or a list posting, never on a message with no recipient outside this server, never twice on one this server already stamped, and never on a message relayed in from outside that merely claims a hosted sender domain. **Still open:** it is a domain-wide footer, not a mail flow rule - no conditions, no per-account text, and a message addressed to both a partner and a colleague gets one footer that the colleague sees too. `hmailserver/docs/DomainTransforms.md` is the walk. |
| ⬜ | **Journaling and dual delivery** (Exchange journaling, Google's dual delivery) | A copy of every message of a domain, or of chosen accounts, to an external address or a second system - compliance archives and migrations both want it. The archive on disk exists; the copy to elsewhere does not. |
| ⬜ | **Remote-domain settings** (Exchange) | Per remote domain: whether automatic replies and forwards may go there, the message format, the TLS required. |
| ⬜ | **Restricted delivery** (Google Workspace's restrict delivery) | Accounts, or a whole domain, that may only exchange mail with named domains - schools and regulated desks. |
| ⬜ | **Allow and block lists for the server** (Microsoft 365's tenant allow/block list) | Senders, domains and URLs allowed or blocked for every account, with an expiry on each entry and the reason kept. |
| ⬜ | **eDiscovery** (Google Vault, Microsoft Purview) | Search across every mailbox and the archive by person, date, words and attachment, the results held or exported as EML or PST with a manifest, and every search logged - the legal-hold rows have the hold; this is the finding. |
| ⬜ | **Roles** (every admin console) | Beyond the one administrator and the domain-scoped API key: a help-desk role that resets passwords and nothing else, a read-only auditor, a domain administrator who signs in to the Deck for their domain. |
| ⬜ | **SCIM provisioning** (Entra ID, Okta, Google) | Accounts created, renamed, suspended and removed by the organisation's identity provider over SCIM 2.0, with the aliases and the lists. The LDAP backend authenticates; SCIM provisions. |
| ⬜ | **SAML, beside OIDC** | The identity providers that still speak only SAML, for the webmail and the Deck sign-in. |
| ⬜ | **Delegated mailboxes announced to Outlook** (Exchange automapping) | A mailbox an account is delegated to appears in Outlook by itself through Autodiscover, as it does with Exchange. |
| ⬜ | **An organisation CA for S/MIME** (Exchange with AD CS) | Certificates issued to every account by the server's own CA and published for lookup, so S/MIME works inside the organisation without anyone buying a certificate. |
| ⬜ | **Gateway encryption to partner domains** | Mail to a named partner domain encrypted at the server with the partner's S/MIME or OpenPGP key, whatever the sender's client - the enterprise shape of the end-to-end section. |
| ⬜ | **A user's data, exported** (GDPR article 20) | Everything the server holds on one person - mail, contacts, settings, logs that name them - as one archive, from the Deck, with the erasure that follows it. |
| ✅ | **`security.txt`** (RFC 9116) | **Done 15 September 2026.** The server half already existed: the web-services listener serves `/.well-known/security.txt` (and the legacy `/security.txt`) for every hosted domain with a postmaster, naming that domain's own contact - the right file for an operator's site, since a report about their server goes to them, not to this project. The repository half is `.well-known/security.txt` at the root: the GitHub Security Advisories address as the contact, the policy `.github/SECURITY.md`, a canonical URL and a one-year expiry, which is what the badge and the scanners look for. |
| ⬜ | **Session policies** | Idle timeout, absolute lifetime, one session per device, and a session bound to the network it began on, as settings; a new sign-in from a new device announced to the recovery address. |

### Deliverability and the mail path

| | Item | Detail |
|---|---|---|
| ⬜ | **IP warm-up** (every sending service) | A schedule for a new outbound address: so many messages a day to each large provider, rising over weeks, enforced by the queue, with the counts on the Deck. |
| ⬜ | **Mandatory TLS per domain** (mailcow's TLS policy map, Exchange connectors) | Outbound mail to a named domain refused rather than sent in the clear when TLS cannot be had or the certificate does not verify; inbound from a named domain likewise - the row that makes REQUIRETLS an administrator's policy rather than a sender's request. |
| ⬜ | **Recipient callout for backup MX** | A backup MX that asks the primary whether an address exists before accepting, so it does not become a backscatter source when the primary is down. |
| ⬜ | **Bounce handling for lists** | Section 7's list manager: VERP addresses, hard bounces unsubscribing, soft bounces counted. |

### Recovery and account safety, for the person

| | Item | Detail |
|---|---|---|
| ⬜ | **Recovery address and codes** (every provider) | A recovery email address and a set of one-time recovery codes, set in the webmail, so a forgotten password or a lost authenticator is a self-service reset and not a ticket. Today only an administrator can reset either. |
| ⬜ | **A breach check on the password** | Section 7. |
| ⬜ | **Passkeys** | Section 7. |

## 11. Learning it: tutorials, tours and in-product help

Everything below is about the first hour, which is the hour this product currently spends worst. A new
administrator meets a Control Panel of forty-seven pages, a Deck of ten views and a webmail, and the help is
a wiki in another window. Nothing here is a mail feature; all of it is why a good mail server gets returned.
The Welcome page's twelve intents and the setup verdict on the dashboard are the ground this builds on.

| | Item | Detail |
|---|---|---|
| ✅ | **Tutorial helper functions** | **Done 15 September 2026.** One mechanism, the same shape in all three surfaces, and the contract for it is a section of `hmailserver/docs/ControlPanelDesign.md`. **A step** is an element - by automation id in the Control Panel, by element id or a stable selector in the two pages - one sentence, an optional condition that ends the step when the thing has happened, and what to do when the element is not there: skipped, or the tour ended *saying which step could not be shown*. A step never acts; the only thing it does besides pointing is open the page its element is on, because an element on a page nobody has opened is in no tree to be found in. **A tour** is an ordered list of those with an id, a name and a version - the id is what the finished record is kept under, the version is what puts a tour that has grown a stop back in front of somebody who finished the old one. **Where it is kept:** one line, `firstrun=1|firstrun@3v1`, the same form in all three - `HKCU\Software\hMailServer\ControlPanel` under `TourProgress` beside the window bounds and the palette's history, `localStorage` under `hmsTours` for the Deck, and the account's preferences under `tours` for the webmail, so a reader who starts the walk on a laptop finishes it on a phone. **Refusable, resumable, and never in the way:** the ring takes no pointer events and the card is the only thing on its layer that does, so every control underneath - the very one being pointed at - stays usable; nothing is focused when a step opens and the Control Panel puts the focus back where it found it; `F6` reaches the card and `Escape` ends the tour only while the keyboard is already inside it, so Escape still belongs to the page everywhere else; the card's four captions are the only ones in the Control Panel with no Alt key, deliberately, because a card that floats over any of forty-seven pages cannot own a key without taking one that page already has. **Accessibility is in it rather than after it:** the card is a polite live region, the Control Panel raises a UI Automation notification with each step's sentence so the walk is usable without sight and without focus being taken, and the highlight is a solid ring in the accent *and* a dashed one inside it, so it survives greyscale, colour blindness and High Contrast. **It is the change that ends a step, not the state** - somebody who already has a domain reads the sentence about domains and presses Next rather than watching the step vanish. **Every sentence goes through the catalogues**: 23 new texts in the Control Panel's seventeen languages, 17 in the Deck's twenty, 14 in the webmail's twenty-three. Where the code is: `Services/Tour.cs`, `Services/TourSession.cs` and `Services/TourProgress.cs` are WPF-free and compiled into `ControlPanel.Core`, as `ConnectionStatus` and `SettingsPageStatus` are, with 39 tests on the model, the stepper and the record (750 from 711); `MainWindow.Tour.cs` and `Views/Scaffold/TourOverlay.cs` are the half that needs a window. The two pages' harnesses execute theirs: `build/deck-script-test.js` 370 checks from 336 and `build/portal-script-test.js` 306 from 274, covering a tour that runs, a step whose element is missing skipped rather than hung on, a condition that moves the tour on by itself and one that does not because it was already true, the finished state remembered, and the whole thing drawn in German. **What is not in it:** a tour cannot branch, and it cannot wait for something the surface has no cheap way to ask about - the conditions are counts and visible state, nothing more. |
| ✅ | **The first-run walk, in each surface** | **Done 15 September 2026**, one per surface, each ending where the person wanted to be rather than where it started. **The Control Panel's** is seven stops: a domain (waiting for one to exist), the domain list, a mailbox (waiting for one), the listeners, a listener that offers TLS (waiting for one), the certificates, and the dashboard's own verdict on what has been built - the stop about the listeners is the one marked as ending the tour if it is not there, because the rest is meaningless without it. **The Deck's** is the same path in a browser, the same seven stops over its domains, ports, certificates and dashboard views, opening each view as it reaches it. **The webmail's** is four: the reading pane (which ends when a message is opened), the compose window (which ends when one is opened), labels, and search. **The ways in:** on the Welcome page a button in the header and, until the walk has been finished, a notice above the twelve intents; in the Ctrl+K palette a row of its own above the page list, ranked ahead of a page of the same name because somebody who types "show me around" has said what they want in so many words; and a help button in the header of any page a tour visits, which starts the walk *at its first stop on that page* - set by the shell from the catalogue, so a page gains the button by being named in a tour rather than by being edited. The Deck's is the `?` button in its header and the webmail's is *Show me around* in the account menu, both behaving the same way. Eight automation ids were added for the steps to point at (`domains-add`, `domains-grid`, `domains-accounts`, `ports-grid`, `ports-add`, `certs-grid`, `dashboard-verdict`, `page-help`); nothing was moved or renamed, which is what keeps the search index, the palette and `SettingsSearchIndexTests` passing. **What is not in it:** the walk points at the certificate list but does not walk somebody through ACME, and there is one walk per surface rather than one per page - a second tour is now a list in `TourCatalog` and a few sentences in the catalogues, which is the point of the row above. |
| ⬜ | **Help where the question is** | A help button on every page that opens what the wiki says about that page, beside the page rather than instead of it, and the same text offline in the binary so a server with no route to the Internet is not a server with no documentation. The Ctrl+K palette already matches a page's purpose sentence; this is the other direction. |
| ⬜ | **A sample domain that can be thrown away** | One command - and one button on the Welcome page - that makes `example.test` with three accounts, a distribution list, a rule and a handful of delivered messages, so that every page has something in it while somebody is learning, and one that removes all of it afterwards and says what it removed. The regression suite builds exactly this for itself several hundred times a day; nothing offers it to a person. |
| ⬜ | **A checklist that knows what is done** | The dashboard says whether the setup is sound; it does not say what to do next. A short list - a domain, an account, TLS on the submission port, SPF, DKIM, DMARC, a backup destination, an administrator second factor - each row checked against the server rather than ticked by hand, each with the page that finishes it. The checks exist behind the diagnostics pages; what is missing is the list. |

## 12. The enterprise questions nothing here answers yet

Section 10 covers most of what a buyer asks. These are the ones it does not, found by walking the same comparison again with a procurement checklist rather than a feature list. Each is genuinely absent, checked against the code rather than remembered.

| | Item | Detail |
|---|---|---|
| ⬜ | **The backup does not carry half of what an account now has** | **Found 15 September 2026, while the wiki was being checked against the code, and it is a defect rather than a gap.** `BackupExecuter` writes what `XMLStore` knows how to write, and nothing reads `hm_contacts`, `hm_accountprefs`, `hm_scheduled`, `hm_files`, `hm_smimekeys`, `hm_calendars` or `hm_calendarobjects`; `Message::XMLStore` writes the flags but not `messagekeywords`, so labels, the second and third stars and follow-up flags are not in the archive either. Every one of those tables cascades from `hm_accounts`, and a restore calls `Domains::DeleteAll()` first - **so restoring a backup taken today deletes an account's address book, its webmail settings, its scheduled messages, its files, its S/MIME keys, its calendar and its labels, and the archive it restores from never held them.** Every one of those stores arrived after the backup was written, and nothing failed loudly when it did. What it needs: each of those tables in the backup and the restore, the keywords on a message, a test per store that takes a backup, deletes the data, restores and finds it there, and a check that fails when a new per-account table is added without one. Until it lands, the wiki says plainly that the database must be backed up with the database server's own tool as well - which is the honest answer and not a fix. |
| ⬜ | **Rooms and equipment** (Exchange resource mailboxes) | A mailbox that is a meeting room or a projector: it accepts or declines an invitation by policy - free at that time, inside the booking window, from someone allowed to book it - rather than by a person reading mail. CalDAV landed on 15 September 2026 and is the ground for it; the row needs the resource account type, the booking policy, and the automatic reply that iMIP will carry. Nothing schedules anything today. |
| ⬜ | **Distribution lists that define themselves** | A list whose members are a query - every account in a domain, everyone with a given directory attribute - evaluated when a message is sent to it rather than maintained by hand. The static list is there, with moderation; the query is not. |
| ⬜ | **Devices, listed and revocable** | Every client that has signed in to an account, by protocol, address and last use, shown to the account holder and to the administrator, with a button that ends the session and revokes the application password behind it. The sessions and application passwords are both there and both listed; what is missing is the device as the thing a person recognises, and the one press that answers "my phone was stolen". |
| ⬜ | **Throttling per destination, and per tenant** | A message to a provider that is deferring should slow down for that provider and for nobody else, and one tenant should not be able to spend the whole server's outbound capacity. Per-account and per-domain sending limits exist; the outbound queue has no per-destination concurrency, no back-off that reads the remote's 4xx text, and no per-tenant share. |
| ⬜ | **Attachments opened somewhere safe** | ClamAV says whether a file is known to be bad. The question an enterprise asks is what an unknown file does when it is opened, which means handing it to a sandbox and holding the message until the answer comes back - a route to an external service, a hold that is not a quarantine, and a verdict that can arrive minutes later. |
| ⬜ | **SNMP** | The metrics are Prometheus and OpenTelemetry, which is right for anything built this decade and wrong for the monitoring system a bank already owns. An SNMP agent answering the mail MIB (RFC 2789) beside them, read-only, off by default. |
| ⬜ | **A migration that survives being interrupted** | The mirror collects one account's IMAP folders and the import tool reads mbox and Maildir. A migration of five hundred mailboxes from Microsoft 365 needs what neither has: a job per mailbox with a state, throttling the source will tolerate, a resume after a network failure or a restart, a report per mailbox, and contacts and calendars beside the mail. |
| ⬜ | **An upgrade nobody has to book a window for** | The installer stops the service, and a live update stops it too. With active-active nodes (section 7) an upgrade is a rolling restart; without them it is still possible to drain the listeners, finish the sessions in flight, apply the schema step and come back - which is minutes of refused connections rather than an outage, and it is not what happens today. |
| ⬜ | **A quota policy, not a quota** | An account has a maximum size. An enterprise wants the warning at eighty per cent in the reader's own language, the grace that still accepts incoming mail for a while, the point at which sending stops before receiving does, and a report of who is near the line - per domain, set once. |
| ⬜ | **The evidence pack** | `hmailserver/docs/RegulatoryScope.md` answers the Cyber Resilience Act and the Product Liability Directive. A buyer's security questionnaire asks a different set of questions - ISO 27001 and SOC 2 control mappings, the SBOM and provenance story in one place, the vulnerability handling policy, the release and signing chain, the data flows and where mail is stored. Every one of those facts exists in this repository; none of them is in a document somebody can send to a procurement team. |

## 13. The settings themselves

Where a setting lives, who may change it, when it takes effect and what it was before. The server has two
tables: `hm_settings`, which the COM `Settings` object and the Control Panel's classic pages have always
used, and `hm_inisettings`, which since schema 6042 holds the 238 keys the `[Settings]` section of
`hMailServer.ini` used to be the store for. **That flip has landed** (the first row below): the database
decides, the file's section is a cache the server rewrites at every start, a value edited into it is named
in the error log and put back, and a `[SettingsOverride]` section is the deliberate door for a database that
is unreachable or wrong. The rows below it are what that store makes possible and was blocking.

| | Item | Detail |
|---|---|---|
| ✅ | **The database is the settings store; the file is the bootstrap** | **Landed at schema 6042.** The rule since 15 September 2026 - a new setting goes in the database, and no new key is added to `[Settings]` - is in `.github/CONTRIBUTING.md` where a contributor meets it, and `build/check-ini-coverage.py` now holds it against a committed baseline (`build/ini-settings-baseline.txt`, 263 keys across five sections): a new `[Settings]` key fails CI with the rule quoted and `hm_settings` named, a new bootstrap key is allowed with the baseline updated in the same commit. The precedence `hm_inisettings` was built for is flipped. `IniSettingStore::Synchronize` reads the table, adopts any `[Settings]` key the table has never seen - which is the migration, done once on the first start at 6042 and idempotent afterwards - and then makes the file match: a key edited in the file is named in the error log (HM5804) and NOT applied, and its line is put back; a row whose line has been deleted is written back rather than dropped, because deleting a line is no longer how a setting is returned to its default (`DeleteIniSetting`, `DELETE /api/v1/settings/ini/{name}` and the Control Panel are). Writes invert with it: `IniSettingStore::WriteSetting` stores the row FIRST and brings the file into line afterwards, because file-first would let a failed row write report success and be reverted at the next start. `[SettingsOverride]` is the door: applied over everything, read even when the table cannot be, and announced by name at every start (HM5807); a store of a key an override shadows says so too (HM5808). The whole `[Settings]` section stays mirrored into the file, and that is not tidiness - `hMailServer.exe /Register` reads `ServiceAccountName` and `ServiceAccountPassword` with no database open at all, because registering the service is what happens before there is one. The Control Panel's `IniFeatureStore` inverts to match: it reads and writes over COM even when the file is on the same machine, and a session-less Control Panel says settings need a connection rather than writing a file that will be undone. The schema step carries no DDL on purpose - the table has been right since 6011 - and the version bump IS the change, because `Application::OnDatabaseConnected` refuses a mismatch in both directions and that is what keeps a 6.3.3 server, which would let the file win, away from a database that is now the store. **Left for the rows below:** `[SendingLimits]`/`[SendingLimitsOverrides]` and `[LDAP]` are still read from the file, outside the `IniFileSettings` seam, and moving them needs section-qualified rows rather than the bare key names `hm_inisettings` holds today; `[GUILanguages]` stays by the rule itself, since `Languages::Load` runs before the database is open. |
| ⬜ | **A setting that takes effect when it is saved** | Today every `[Settings]` key is latched into a typed member when the service starts, so a change is persisted now and applied at the next start - which is what the Control Panel means by "applies after a service restart", and what an administrator reads as "this product cannot be configured while it is running". With the store in the database a save can publish: `LoadSettings` re-runs behind a lock, each subsystem that can take a new value takes it, and the ones that genuinely cannot - a listener's port under a live socket, the database connection itself - are marked in the settings index and keep the restart they need, with `POST /api/v1/server/reinitialize` as the way to take them without stopping the service. The index that would carry that mark is generated already (`build/generate-settings-index.ps1`). |
| ⬜ | **One value, three levels** | A handful of settings can already be set per domain (the relay, the limits, the signature); most are server-wide because that is where they were written. The shape people expect from a hosted product is server, then domain, then account, each overriding the last, with the **effective** value and where it came from shown beside the field - which is also the only honest way to answer "why is this account not doing what the domain says". Needs the store above first: a per-domain value has nowhere to live in a file that has one section. |
| ⬜ | **What changed, when, and who changed it** | Section 7's audit trail covers administrative actions; this is the settings half of it, and it is what a change-control process asks for: the key, the value before, the value after, the administrator, the address they connected from, and the ability to see the last fifty changes on a page or to put one back. A key-value table with history is a table with a second table beside it, and the REST layer already knows which route wrote what. |
| ⬜ | **What this server has changed from stock** | One page, and one line of `hmctl`, that lists every setting whose value is not the shipped default, with the default beside it. It is the first thing anybody asks when a server behaves oddly, the first thing to put in a support message, and the honest diff between two installations. Every default is already in the code as the third argument of the read; the list is what is missing. |
| ⬜ | **Configuration from the environment, for every setting** | The container image takes about twenty settings as `HM_*` environment variables and writes them into the ini on start; a Kubernetes user expects that of every setting, and expects a Secret to be able to supply any of them as a file. With the database as the store the entrypoint seeds the database instead of rewriting a file, which also settles what happens when the environment and the stored value disagree: the environment is the deployment's intent and wins on start, and the log says which values it overrode. |
| ⬜ | **Profiles, so a new server starts in the right shape** | The defaults suit one kind of deployment. A named profile - a small office server, a server behind a filtering gateway, an outbound relay, a high-volume sender - applied at setup or from the Deck, that sets thirty settings at once and says exactly which thirty and what they were, with a diff before it is applied and a way back. The setup wizard asks none of this today, and a new administrator is left to find each one. |

## 14. What a mail platform is asked to do, and this one does not

A third sweep, this time from the question an integrator asks rather than the one an administrator or a
reader asks: what does an application, a deliverability team or a compliance officer need from the server
that it cannot get today? Each row was checked against the code before it was written, and each says what
exists so that the gap is the only claim in it.

| | Item | Detail |
|---|---|---|
| ⬜ | **Mail in, to a webhook** (SendGrid and Mailgun call it inbound parse) | The outbound half is section 7's webhooks row. This is the other direction and it is what an application actually buys a mail server for: an address, or a whole domain, whose mail is parsed - sender, recipients, subject, text, HTML, attachments, the verdicts - and posted as JSON to a URL, with the same signature, retries and dead letter as an outbound webhook, and with a size ceiling and an attachment policy of its own. The rule already has "run a script"; nothing has "post it somewhere". |
| ⬜ | **A send API for applications** | `POST /api/v1/me/messages` sends as the signed-in account and is the webmail's. What an application wants is a server-level route with an API key of its own, a per-key sender it may use and may not exceed, per-key rate limits and a queue it can ask about afterwards - "was message X delivered, deferred or bounced, and why" - which is the message trace the server already keeps, per key. Without it every application that wants to send through this server speaks SMTP with an application password, which works and tells them nothing. |
| ⬜ | **Bounces read, and a suppression list** | The server retries and gives up; it does not read the DSN that comes back. A bounce classified (a mailbox that does not exist, a mailbox that is full, a policy refusal, a temporary failure) against RFC 3463's codes, counted per address, and an address that has hard bounced put on a suppression list that the next send is checked against - per domain, visible, and removable by hand. Sending to a known-dead address is how a sender's reputation is spent. |
| ⬜ | **Complaints, read** (the feedback loops) | The large mailbox providers send an abuse report (RFC 5965, `message/feedback-report`) when a recipient presses "this is spam". This server has nowhere for one to arrive: a mailbox per domain that parses them, counts them by sending account and campaign, adds the complainant to the suppression list above, and warns when the rate crosses the threshold the provider publishes. Deliverability is mostly this. |
| ⬜ | **DKIM keys that rotate** | A key is generated once, written to a file and used until somebody remembers. What is needed is the ordinary practice: two selectors live at once, a new key published and given time to propagate, signing moved to it, the old one kept for the reports that arrive late and then removed - on a schedule, per domain, with the DNS the administrator must publish shown at each step and checked from the resolver before the switch. The DNS-record view already knows how to show and verify a record. |
| ⬜ | **A backup that is encrypted** | The backup writes an archive with the system 7-Zip and verifies it by extracting it; nothing encrypts it. An archive that holds every mailbox on the server is the most concentrated copy of the data there is, and it is written to whatever destination an administrator typed - often a share, sometimes a cloud mount. A passphrase or a key, the archive encrypted as it is written, and the restore asking for it, with the key never stored beside the archive. |
| ⬜ | **A calendar shared with somebody** | CalDAV landed on 15 September 2026 with one calendar per account and no way to give another person sight of it. What an organisation asks for next, in order: free-busy for everyone in the domain (which is also what a room booking needs, section 12), a calendar shared read-only with a named account or a group, and a delegate who may write it and answer invitations on the owner's behalf. The WebDAV ACL layer the address books and calendars sit on is where it goes. |
| ⬜ | **An out-of-office policy, not just a message** | An account has an automatic reply, a subject, a body and an expiry date. An organisation needs the policy around it: a reply to people inside the organisation only, or a different text for them; one reply per sender per period rather than per message; no reply to a list, an automatic message or a null sender (some of which the delivery path already refuses); a default set for the domain; and an administrator able to set one for somebody who has left without signing in as them. |
