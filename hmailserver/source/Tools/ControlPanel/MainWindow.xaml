<ui:FluentWindow
    x:Class="hMailServer.ControlPanel.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    FontFamily="Segoe UI Variable Text, Segoe UI"
    Title="hMailServer Control Panel"
    Width="1280" Height="800"
    MinWidth="760" MinHeight="520"
    WindowBackdropType="Mica"
    ExtendsContentIntoTitleBar="True"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="hMailServer Control Panel" ShowMaximize="True" ShowMinimize="True">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="Mail24" Foreground="{StaticResource BrandBrush}" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.24*" MinWidth="200" MaxWidth="260" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Brand -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="18,10,0,12">
                    <Border Width="34" Height="34" CornerRadius="9" Background="{StaticResource BrandGradient}">
                        <TextBlock Text="hM" FontWeight="SemiBold" FontSize="14" Foreground="White"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="hMailServer" FontWeight="SemiBold" FontSize="14"
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                        <TextBlock Text="Control Panel" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    </StackPanel>
                </StackPanel>

                <!-- Search entry point. The palette was previously reachable only
                     by knowing that Ctrl+K exists, which is not a discoverable
                     interface; this is a normal tab stop that opens the same
                     palette and names the shortcut while it does so. -->
                <ui:Button x:Name="SearchButton" Grid.Row="1" Margin="14,0,14,10"
                           HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                           Padding="10,7" Click="Search_Click"
                           AutomationProperties.AutomationId="nav-search"
                           AutomationProperties.Name="Search pages, settings and tasks. Keyboard shortcut Control K."
                           ToolTip="Search pages, settings and tasks (Ctrl+K)">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ui:SymbolIcon Symbol="Search24" FontSize="14" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="Search" FontSize="14" Margin="8,0,0,0"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBlock Grid.Column="2" Text="Ctrl+K" FontSize="12" VerticalAlignment="Center"
                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    </Grid>
                </ui:Button>

                <!-- Navigation tree. Built from Services/NavigationMap, which is
                     grouped by what an administrator is trying to achieve; the
                     structure is data so that it can be tested. -->
                <TreeView x:Name="NavTree" Grid.Row="2" Background="Transparent" BorderThickness="0"
                          Margin="8,0" SelectedItemChanged="NavTree_SelectedItemChanged"
                          AutomationProperties.Name="Main navigation"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="Padding" Value="6,6" />
                            <Setter Property="Margin" Value="0,1" />
                            <!-- Fluent Body (Typography.Body). The rows sat at 13
                                 through the ramp re-basing, a half-step off both
                                 rungs and the actual size of most of the sidebar. -->
                            <Setter Property="FontSize" Value="14" />
                            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="HorizontalContentAlignment" Value="Left" />
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TreeViewItem">
                                        <StackPanel>
                                            <Border x:Name="Bd" CornerRadius="6" Cursor="Hand"
                                                    Background="{TemplateBinding Background}" SnapsToDevicePixels="True"
                                                    BorderThickness="1.5" BorderBrush="Transparent">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="18" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <Rectangle x:Name="Pill" Grid.Column="0" Width="3" Height="15"
                                                               RadiusX="1.5" RadiusY="1.5" HorizontalAlignment="Left"
                                                               Margin="1,0,0,0" Fill="{StaticResource BrandBrush}" Opacity="0" />
                                                    <ToggleButton x:Name="Expander" Grid.Column="0" ClickMode="Press"
                                                                  Style="{StaticResource NavExpanderToggle}"
                                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" />
                                                    <ContentPresenter x:Name="PART_Header" Grid.Column="1" ContentSource="Header"
                                                                      HorizontalAlignment="Left" VerticalAlignment="Center"
                                                                      Margin="{TemplateBinding Padding}" />
                                                </Grid>
                                            </Border>
                                            <ItemsPresenter x:Name="ItemsHost" Margin="10,0,0,0" />
                                        </StackPanel>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsExpanded" Value="False">
                                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
                                            </Trigger>
                                            <Trigger Property="HasItems" Value="False">
                                                <Setter TargetName="Expander" Property="Visibility" Value="Collapsed" />
                                            </Trigger>
                                            <Trigger SourceName="Bd" Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                                                <Setter TargetName="Pill" Property="Opacity" Value="1" />
                                            </Trigger>
                                            <Trigger Property="IsKeyboardFocused" Value="True">
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AppBrandBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="HasItems" Value="True">
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TreeView.ItemContainerStyle>
                </TreeView>

                <!-- Footer -->
                <StackPanel Grid.Row="3" Margin="18,0,18,14">
                    <!-- The connection indicator. The dot was a literal #3FB950,
                         which is the dark theme's success green painted regardless
                         of theme: on a High Contrast desktop it is an off-palette
                         green the user has told Windows not to show them, and it is
                         the only thing on the window that says "connected" - the
                         text beside it says "user @ host" and never says what the
                         relationship is. The fill now follows AppSuccessBrush, which
                         ThemeTokens recomputes from SystemColors under High
                         Contrast, and ConnText carries the statement in words in its
                         accessible name (set in OnConnected). -->
                    <StackPanel x:Name="ConnBadge" Orientation="Horizontal" Visibility="Collapsed">
                        <Ellipse Width="8" Height="8" Fill="{DynamicResource AppSuccessBrush}" VerticalAlignment="Center" />
                        <TextBlock x:Name="ConnText" Margin="7,0,0,0" FontSize="12"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                    <Grid Margin="0,6,0,0">
                        <TextBlock x:Name="VersionText" FontSize="12" Margin="15,4,0,0" VerticalAlignment="Center"
                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                        <!-- The icon, tool tip and accessible name are owned by
                             UpdateThemeToggle_ in code-behind, which points all
                             three at the state a click will produce (sun while
                             dark, moon while light) and re-points them when the
                             OS flips the theme. The XAML icon is only a
                             placeholder for the instant before ApplySavedTheme
                             runs; a fixed DarkTheme24 here once sat on the
                             button in both states, disagreeing with its verb
                             half the time. -->
                        <ui:Button x:Name="ThemeButton" Padding="7,3"
                                   Icon="{ui:SymbolIcon WeatherMoon24}"
                                   HorizontalAlignment="Right"
                                   Click="Theme_Click" />
                    </Grid>
                </StackPanel>
            </Grid>

            <!-- Content -->
            <Border Grid.Column="1" Background="{DynamicResource ApplicationBackgroundBrush}"
                    CornerRadius="8,0,0,0" Margin="0,4,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Breadcrumb and signposts. A palette jump used to replace
                         the page with no indication of where the user had landed,
                         so the palette taught nothing and had to be used again
                         next time. The trail names the group, the group segment
                         reveals it in the tree, and the "Related" links carry the
                         subjects that genuinely span several pages (transport
                         security is one question to the user and five pages to
                         us) without duplicating a page into two branches. -->
                    <Border x:Name="BreadcrumbBar" Grid.Row="0" Visibility="Collapsed"
                            Padding="26,9,20,8" BorderThickness="0,0,0,1"
                            BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                            AutomationProperties.AutomationId="breadcrumb-bar"
                            AutomationProperties.Name="Location">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <WrapPanel x:Name="BreadcrumbHost" Orientation="Horizontal"
                                       VerticalAlignment="Center"
                                       AutomationProperties.AutomationId="breadcrumb" />
                            <WrapPanel x:Name="RelatedHost" Grid.Column="1" Orientation="Horizontal"
                                       VerticalAlignment="Center" HorizontalAlignment="Right"
                                       AutomationProperties.AutomationId="breadcrumb-related" />
                        </Grid>
                    </Border>

                    <ContentControl x:Name="ContentHost" Grid.Row="1" AutomationProperties.Name="Page content" />
                </Grid>
            </Border>
        </Grid>

        <ui:SnackbarPresenter Grid.Row="1" x:Name="RootSnackbar"
                              VerticalAlignment="Bottom" HorizontalAlignment="Center"
                              Margin="0,0,0,24" />
    </Grid>
</ui:FluentWindow>
