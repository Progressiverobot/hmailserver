<!--
  https://www.progressiverobot.com
  Copyright (c) 2026 Christopher Holloway / Progressive Robot Ltd
  SPDX-License-Identifier: AGPL-3.0-or-later
-->
<UserControl
    x:Class="hMailServer.ControlPanel.Views.LogsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="clr-namespace:hMailServer.ControlPanel.Services"
    xmlns:scaffold="clr-namespace:hMailServer.ControlPanel.Views.Scaffold"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Grid Margin="{StaticResource AppPagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <scaffold:PageHeader x:Name="Header" Title="{loc:L 'Live logs'}"
                             Subtitle="{loc:L 'Streaming the server log in real time.'}">
            <scaffold:PageHeader.Actions>
                <StackPanel Orientation="Horizontal">
                    <ui:Button x:Name="PauseButton" Content="{loc:L '_Pause'}" Click="Pause_Click" Margin="0,0,8,0">
                        <ui:Button.Icon><ui:SymbolIcon x:Name="PauseButtonIcon" Symbol="Pause24" /></ui:Button.Icon>
                    </ui:Button>
                    <ui:Button Content="{loc:L '_Clear'}" Click="Clear_Click">
                        <ui:Button.Icon><ui:SymbolIcon Symbol="Broom24" /></ui:Button.Icon>
                    </ui:Button>
                </StackPanel>
            </scaffold:PageHeader.Actions>
        </scaffold:PageHeader>

        <!-- The log's severity palette (Log*Brush) is the viewer's own and not a
             design token; each line holds one of ThemeTokens' shared brushes, which
             are recoloured in place on a theme change. -->
        <scaffold:Card Grid.Row="1" Padding="4">
            <ListBox x:Name="LogList" Background="Transparent" BorderThickness="0"
                     FontFamily="Cascadia Mono, Consolas" FontSize="12"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                     AutomationProperties.Name="{loc:L 'Live logs'}">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="6,0" />
                        <Setter Property="MinHeight" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="BorderThickness" Value="0" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Text}" Foreground="{Binding Brush}" TextWrapping="NoWrap"
                                   LineHeight="17" LineStackingStrategy="BlockLineHeight" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </scaffold:Card>
    </Grid>
</UserControl>
