<!-- Copyright (c) 2026 Christopher Holloway / Progressive Robot Ltd and the hMailServer contributors
     SPDX-License-Identifier: AGPL-3.0-or-later -->
<UserControl
    x:Class="hMailServer.ControlPanel.Views.RulesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Grid Margin="26,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1.3*" />
        </Grid.RowDefinitions>

        <StackPanel x:Name="HeaderPanel">
            <TextBlock Text="Global rules" Style="{StaticResource PageTitle}" />
            <TextBlock x:Name="SubtitleText"
                       Text="Server-wide message rules, evaluated top to bottom."
                       Style="{StaticResource PageSubtitle}" />
            <ui:TextBox x:Name="SearchBox" PlaceholderText="Search rules..." Width="300"
                        HorizontalAlignment="Left" Margin="0,10,0,0" TextChanged="Search_TextChanged"
                        AutomationProperties.Name="Search rules">
                <ui:TextBox.Icon><ui:SymbolIcon Symbol="Search24" /></ui:TextBox.Icon>
            </ui:TextBox>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="6">
            <DataGrid x:Name="RuleGrid" AutoGenerateColumns="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="None"
                      Background="Transparent" BorderThickness="0" RowHeight="34"
                      SelectionMode="Single" SelectionChanged="RuleGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding Position}" Width="50" />
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Enabled" Binding="{Binding Enabled}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,12">
            <ui:Button Content="Move _up" Click="MoveUp_Click" Margin="0,0,8,0" />
            <ui:Button Content="Move _down" Click="MoveDown_Click" Margin="0,0,8,0" />
            <ui:Button Content="Ena_ble / disable" Click="Toggle_Click" Margin="0,0,8,0" />
            <ui:Button Content="De_lete rule" Appearance="Danger" Click="Delete_Click" Margin="0,0,24,0" />
            <ui:TextBox x:Name="NewRuleName" PlaceholderText="New rule name" AutomationProperties.Name="New rule name" Width="180" Margin="0,0,8,0" />
            <ui:Button Content="_Add rule" Appearance="Primary" Click="AddRule_Click" />
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Criteria -->
            <Border Style="{StaticResource Card}" Margin="0,0,6,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="IF" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center" Margin="0,0,12,0" />
                        <ComboBox x:Name="MatchMode" FontSize="12" Width="180"
                                  SelectionChanged="MatchMode_SelectionChanged" />
                    </StackPanel>
                    <DataGrid Grid.Row="1" x:Name="CriteriaGrid" AutoGenerateColumns="False" IsReadOnly="True"
                              HeadersVisibility="None" GridLinesVisibility="None"
                              Background="Transparent" BorderThickness="0" RowHeight="30"
                              SelectionMode="Single" MouseDoubleClick="EditCriterion_Click">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Description}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                        <ui:Button Content="Add _criterion…" Appearance="Primary" Click="AddCriterion_Click" Margin="0,0,8,0" />
                        <ui:Button Content="_Edit criterion…" Click="EditCriterion_Click" Margin="0,0,8,0" />
                        <ui:Button Content="_Remove criterion" Appearance="Danger" Click="RemoveCriterion_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Actions -->
            <Border Grid.Column="1" Style="{StaticResource Card}" Margin="6,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="THEN" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8" />
                    <DataGrid Grid.Row="1" x:Name="ActionsGrid" AutoGenerateColumns="False" IsReadOnly="True"
                              HeadersVisibility="None" GridLinesVisibility="None"
                              Background="Transparent" BorderThickness="0" RowHeight="30"
                              SelectionMode="Single" MouseDoubleClick="EditAction_Click">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Description}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                        <ui:Button Content="Add ac_tion…" Appearance="Primary" Click="AddAction_Click" Margin="0,0,8,0" />
                        <ui:Button Content="Ed_it action…" Click="EditAction_Click" Margin="0,0,8,0" />
                        <ui:Button Content="Re_move action" Appearance="Danger" Click="RemoveAction_Click" Margin="0,0,8,0" />
                        <ui:Button Content="U_p" Click="ActionUp_Click" Margin="0,0,8,0" />
                        <ui:Button Content="Dow_n" Click="ActionDown_Click" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
