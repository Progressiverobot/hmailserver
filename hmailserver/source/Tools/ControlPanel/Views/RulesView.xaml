<UserControl
    x:Class="hMailServer.ControlPanel.Views.RulesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Grid Margin="26,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1.3*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Global rules" Style="{StaticResource PageTitle}" />
            <TextBlock x:Name="SubtitleText"
                       Text="Server-wide message rules, evaluated top to bottom."
                       Style="{StaticResource PageSubtitle}" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="6">
            <DataGrid x:Name="RuleGrid" AutoGenerateColumns="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="None"
                      Background="Transparent" BorderThickness="0" RowHeight="34"
                      SelectionMode="Single" SelectionChanged="RuleGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding Position}" Width="50" />
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Enabled" Binding="{Binding Enabled}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,12">
            <ui:Button Content="Move up" Click="MoveUp_Click" Margin="0,0,8,0" />
            <ui:Button Content="Move down" Click="MoveDown_Click" Margin="0,0,8,0" />
            <ui:Button Content="Enable / disable" Click="Toggle_Click" Margin="0,0,8,0" />
            <ui:Button Content="Delete rule" Appearance="Danger" Click="Delete_Click" Margin="0,0,24,0" />
            <ui:TextBox x:Name="NewRuleName" PlaceholderText="New rule name" AutomationProperties.Name="New rule name" Width="180" Margin="0,0,8,0" />
            <ui:Button Content="Add rule" Appearance="Primary" Click="AddRule_Click" />
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Criteria -->
            <Border Style="{StaticResource Card}" Margin="0,0,6,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="IF" FontWeight="Bold" FontSize="15" VerticalAlignment="Center" Margin="0,0,12,0" />
                        <ComboBox x:Name="MatchMode" FontSize="12.5" Width="180"
                                  SelectionChanged="MatchMode_SelectionChanged" />
                    </StackPanel>
                    <DataGrid Grid.Row="1" x:Name="CriteriaGrid" AutoGenerateColumns="False" IsReadOnly="True"
                              HeadersVisibility="None" GridLinesVisibility="None"
                              Background="Transparent" BorderThickness="0" RowHeight="30"
                              SelectionMode="Single" MouseDoubleClick="EditCriterion_Click">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Description}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                        <ui:Button Content="Add…" Appearance="Primary" Click="AddCriterion_Click" Margin="0,0,8,0" />
                        <ui:Button Content="Edit…" Click="EditCriterion_Click" Margin="0,0,8,0" />
                        <ui:Button Content="Remove" Appearance="Danger" Click="RemoveCriterion_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Actions -->
            <Border Grid.Column="1" Style="{StaticResource Card}" Margin="6,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="THEN" FontWeight="Bold" FontSize="15" Margin="0,0,0,8" />
                    <DataGrid Grid.Row="1" x:Name="ActionsGrid" AutoGenerateColumns="False" IsReadOnly="True"
                              HeadersVisibility="None" GridLinesVisibility="None"
                              Background="Transparent" BorderThickness="0" RowHeight="30"
                              SelectionMode="Single" MouseDoubleClick="EditAction_Click">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Description}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                        <ui:Button Content="Add…" Appearance="Primary" Click="AddAction_Click" Margin="0,0,8,0" />
                        <ui:Button Content="Edit…" Click="EditAction_Click" Margin="0,0,8,0" />
                        <ui:Button Content="Remove" Appearance="Danger" Click="RemoveAction_Click" Margin="0,0,8,0" />
                        <ui:Button Content="Up" Click="ActionUp_Click" Margin="0,0,8,0" />
                        <ui:Button Content="Down" Click="ActionDown_Click" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
